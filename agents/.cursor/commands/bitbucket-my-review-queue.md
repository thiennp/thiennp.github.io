# Bitbucket: my review queue

Drain your assigned Bitbucket PR review queue using the team workflow (scripts + prompt).

Follow: **`.cursor/docs/workflows/playbooks/git-bitbucket/bitbucket-my-review-queue.md`**.

Bitbucket/Jira helpers from repo root: **`npm --prefix .cursor/tools run pm:bb-pr-changes`**, **`pm:bb-add-pr-review`**, etc. — see **`.cursor/tools/package.json`**. Credentials: **`frontend/.env`**.

Orientation: **`.cursor/skills/bitbucket-pr-workflows/SKILL.md`**.
