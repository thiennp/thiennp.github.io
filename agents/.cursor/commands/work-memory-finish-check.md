# Work memory finish check

Run at the end of a **big enough** task to avoid repeating past mistakes.

1. Re-read `/Users/thien.nguyen/thiennp.github.io/agents/work-memory/mistakes/INDEX.md`
2. Compare what you just did against pinned + related entries
3. If you discovered a new gotcha/preference: run the `/work-memory-log` workflow now
4. If an existing entry is wrong/outdated: mark `status: superseded` and link the replacement
5. Confirm in one short line: **Finish check:** `ok` | `logged <id>` | `updated <id>`

Skip only for tiny one-liner changes with no reusable learning.
