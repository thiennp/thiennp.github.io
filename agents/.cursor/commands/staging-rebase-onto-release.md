# Git: staging rebase onto release

Align local and remote **`staging`** with **`release`** (fetch, rebase, dedupe, JIRA keys in messages, push policy).

Follow: **`.cursor/docs/workflows/playbooks/git-bitbucket/staging-rebase-onto-release.md`**.

Execute git steps from **repository root** when acting as the agent; state blockers if a branch or permission is missing.

Orientation: **`.cursor/skills/git-staging-and-split-commits/SKILL.md`**. Project key **POST-*** per **`.cursor/rules/jira-ticket-management.mdc`**.
