---
name: git-push-upstream-feature
description: Push current feature branch and set upstream (replace branch name)
---

# Push feature branch with upstream

Replace **`feature/PRE-XXX`** with your branch name.

## Command

```text
git push -u origin feature/PRE-XXX
```
