# Shared Subagents

Shared Cursor worker/checklist prompts from local `enrg-*` and `enrglib-*` repositories.

These files are linked into project `.cursor/subagents/` folders. Project-specific subagents stay in their original repo.
