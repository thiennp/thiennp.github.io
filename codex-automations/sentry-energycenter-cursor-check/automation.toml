version = 1
id = "sentry-energycenter-cursor-check"
kind = "heartbeat"
name = "Sentry Cursor alternating checks"
prompt = "Every heartbeat is a scheduler tick for independent Sentry/Jira/Cursor workflows. Do not use one global lock. At the start, build a lightweight view of active workflow locks from this thread and the two repos: Jira ticket key, Sentry issue key, target repo, branch, phase, and whether a Cursor/repo verification/staging/Jira transition is still active. A workflow lock only blocks work that would touch the same repo/branch/ticket/Sentry issue or the same unsafe shared operation. It must not block unrelated triage or a different target repo.\n\nAllowed concurrency: multiple workflows may exist at the same time if they are isolated. It is OK to start a new triage round while an older workflow is waiting on PR review, staging follow-up, Jira transition, or user-visible Cursor state, provided the new round will not reuse the same Jira ticket, Sentry issue, branch, or mutable repo state. Never start two Cursor fixes concurrently in the same repo/worktree. Do not interrupt, reset, checkout over, kill, or overwrite an existing workflow. If a repo already has an active Cursor fix, verification, cherry-pick, or uncommitted workflow changes, do not start another fix in that same repo; either continue that workflow if it is the intended next step or start/scan the other target repo if safe. If both target repos are busy, return a quiet status.\n\nWhen no conflicting workflow lock exists, start one new automation round. A round owns only its ticket/Sentry/repo/branch scope until it reaches a terminal outcome: no actionable issue found after feed scan, non-actionable skips exhausted, blocker needing user attention, Cursor no-change result, verification failure, successful fix through PR/staging/Jira transitions, or merged-PR cleanup completion. When a round reaches terminal outcome, immediately start another safe non-conflicting round from the opposite target when tools/time permit; do not wait for the next scheduled heartbeat. Scheduled heartbeats should only discover whether there is capacity for another safe workflow.\n\nTarget order: use current_time_iso only for the first new round started by a scheduler tick. Compute the 5-minute slot; even slots start with EnergyCenter Reverse, odd slots start with Result List Goes React. Chained rounds alternate targets from the just-finished round. If slot cannot be determined, alternate from the last successful completed round, otherwise start EnergyCenter Reverse. If the selected target repo is busy, check the other target instead; if both are busy, do nothing quietly.\n\nAccess/config precedence: for all Jira, Bitbucket, Sentry, PR, and project workflow access, first inspect and use the target repo's own `.cursor` folder. Result List Goes React sources include `/Users/thiennguyen/docker-localdev2/workdir/result-list-goes-react/.cursor/mcp.json`, `.cursor/scripts/.env`, `.cursor/scripts/jira/.env`, `.cursor/scripts/bb/.env`, Jira scripts/clients under `.cursor/scripts/jira/`, `.cursor/commands/`, `.cursor/skills/`, and `.cursor/rules/`. EnergyCenter Reverse sources include `/Users/thiennguyen/docker-localdev2/workdir/enrg-energycenter-rev/.cursor/cli/.env`, `.cursor/cli/scripts/.env`, `.cursor/cli/jira/JiraClient.ts`, `.cursor/cli/scripts/jira.ts`, `.cursor/commands/bitbucket-create-pr.md`, `.cursor/commands/pm-jira-bitbucket-cli.md`, and `.cursor/rules/`. Use global Codex MCP servers/wrappers only when project-local `.cursor` sources do not provide the needed access or command. Never display or copy secrets from `.cursor`, env files, MCP configs, cookies, tokens, passkeys, OTPs, or raw credential material.\n\nDo not ask the user for permission to run terminal commands for Jira, Bitbucket, or Sentry access. Do not run ad hoc Codex shell/API commands for Jira/Bitbucket/Sentry when they would need external network or credential access. Instead, delegate those operations to the project-local Cursor workflow/scripts by asking Cursor in the native macOS Cursor app to run the repo's documented `.cursor` command, skill, or script. Codex may inspect local non-secret docs/config, open Chrome for visible confirmation, and use local git/repo status commands, but credentialed Jira/Bitbucket/Sentry API work should be performed by Cursor/project scripts unless explicitly instructed otherwise.\n\nUse MCP/API/CLI first through the project-local Cursor workflow, then authenticated Google Chrome UI only when API/CLI is unavailable, incomplete, or visible confirmation is required. Cursor work must happen in the native macOS Cursor app/local Cursor context so branch, files, diffs, and workflow output are visible. Prefer normal visible macOS Cursor workflow if headless `cursor agent` has keychain/auth issues.\n\nAt the start of each safe round, run merged-PR cleanup for Jira tickets created or reused by this automation, including PRE-4176, PRE-4177, PRE-4179, PRE-4181, PRE-4184, POST-2506, POST-2507, POST-2508, and future PRE/POST tickets from this workflow. Ask Cursor/project-local scripts to find related PRs and linked Sentry issues. If a related PR is merged, ask Cursor/project-local Jira/Sentry workflow to close/complete the Jira ticket and resolve the linked Sentry issue. Only act on tickets clearly tied to Sentry issues handled here. If no PR is merged or no safe cleanup action exists, continue normal triage. Cleanup for one ticket must not block unrelated triage unless it mutates the same repo/branch/ticket.\n\nFor the chosen target, scan unresolved Sentry issues in Last Seen order until one actionable unowned/unticketed issue is found or the listed/visible feed is exhausted. If the top row is not actionable, continue directly to the next row. Skip and briefly report rows that are already assigned/owned, already ticketed, have a matching open/non-closed Jira found by search, have already been fixed/PR'd/merged/resolved, are known no-fix/noise, are outside target project, or otherwise need no Cursor fix. Do not stop the feed scan for ordinary ownership/ticket de-duplication.\n\nBefore asking Cursor to diagnose/fix a candidate, open/fetch Sentry issue detail and inspect assignee plus Jira/issue-tracking state. If already assigned before this run or already ticketed, skip that row. Only if unassigned and unticketed, assign Sentry to Thien Nguyen using visible Sentry controls or project-local Cursor/Sentry tooling, collect non-secret context, ask Cursor/project-local Jira tooling to search for matching open/non-closed tickets, then create a Jira ticket only if none exists. Result List creates PRE tickets in PRE - Presales. EnergyCenter Reverse creates POST tickets in POST - Postsales. Ticket title must contain Sentry key and short error signature; description must include non-secret Sentry context and Sentry URL. Assign ticket to Thien Nguyen.\n\nAfter creating a ticket in this run, link it back to Sentry using project-local tooling/API via Cursor or visible controls. If visible Sentry/Jira linking remains stuck but the new Jira ticket exists, is assigned to Thien Nguyen, and contains Sentry URL/context, continue to Cursor and record `Sentry/Jira visible link pending`. Move the ticket to In Progress before Cursor starts, using Cursor/project-local Jira workflow rather than Codex ad hoc API calls.\n\nWhen Cursor starts, include full Sentry context, Jira key, Jira In Progress status, and Sentry-link status. Tell Cursor not to paste secrets and to use the Jira key for branch naming, docs, commit, and PR context. Use the repo's `.cursor/commands/fix-bug.md` and linked docs/rules. After a real fix, update required docs, run verification, commit with the Jira key using normal hooks, push the fix branch, and create/attempt PR using the repo's documented Cursor PR workflow such as `/bitbucket-create-pr` or `pnpm bb:create-pr`. Do not use `00000`, do not bypass hooks, do not commit on release, do not push release, and do not create release-to-release PRs.\n\nAfter PR attempt, move Jira to Code Review through Cursor/project-local Jira workflow. Then checkout latest staging, update from origin, cherry-pick fix commit(s), resolve clear conflicts, run required staging verification, push staging, and move Jira to In Test through Cursor/project-local Jira workflow. If Cursor finds no file changes, do not commit, push, PR, or cherry-pick; summarize no commit needed.\n\nStop only on blockers or completed outcomes scoped to that workflow: Sentry assignment failure for an otherwise actionable issue, Jira creation/assignment/status failure, Cursor no-change result after starting, Cursor/verification failure, PR/auth setup block, staging cherry-pick/conflict/verification/push failure, unsafe merged-PR cleanup, or a result requiring user attention. A blocker in one workflow must not stop safe unrelated workflows.\n\nEnergyCenter Reverse target: Sentry project 4511296882540544; expected keys ENRG-ENERGYCENTER-REV; Chrome fallback https://check24-energie.sentry.io/issues/?project=4511296882540544&statsPeriod=24h; repo `/Users/thiennguyen/docker-localdev2/workdir/enrg-energycenter-rev`.\n\nResult List Goes React target: Sentry project 4508879690792960; project result-list-goes-react / enrg-web-frontend; Chrome fallback https://check24-energie.sentry.io/issues/?project=4508879690792960&statsPeriod=24h; repo `/Users/thiennguyen/docker-localdev2/workdir/result-list-goes-react`.\n\nAfter each workflow step/round/chain finishes, summarize active workflow locks, Sentry assignment, skipped rows/reasons, de-duplication, Jira ticket/link/assignment, Sentry/Jira linking or pending status, Jira transitions, changed files, docs updates, commit, push, PR link/result/merge state, staging cherry-pick/verification/push, Sentry resolve result, and Sentry issue-detail context used."
status = "ACTIVE"
rrule = "RRULE:FREQ=MINUTELY;INTERVAL=3"
target_thread_id = "019dd84f-8829-7f92-bdcd-0f7f3a8c4b9d"
created_at = 1777451936420
updated_at = 1778134725515
