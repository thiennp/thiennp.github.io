version = 1
id = "fix-slack-security-dependency-threads"
kind = "cron"
name = "Daily-vulnerabilities-fix"
prompt = "Run mode policy:\n- This automation runs once per day.\n- At the very start of every automation run, ensure the live report service is on by running `/Users/thien.nguyen/.codex/automations/fix-slack-security-dependency-threads/history-dashboard/ensure_live_report_service.sh`; do this before PR polling, blocked-item rechecks, or dashboard scans.\n- On each daily run, use this order: ensure live report service is on, read a frozen memory snapshot, poll pending security PRs/tickets tracked in memory, re-check every blocked Jira ticket and blocked memory item, then start any new dashboard scan or new remediation work.\n- On every run, always re-check every previously blocked Jira ticket and blocked memory item before starting any new dashboard scan or new remediation work.\n- A blocked item must never be assumed still blocked from old evidence; each run must gather fresh evidence using the same scanner/registry/API path that created the blocker when available, or record the fresh access failure if not available.\n- If a pending PR is approved, move its Jira issue to Approved and notify Thien Nguyen in output.\n- Perform full security dashboard scan/fix flow at 09:00 local time (or when explicitly triggered manually).\n- Daily run executes full scan/fix and Jira/PR tracking flow.\n\nSubagent delegation workflow (mandatory for non-trivial daily runs):\n- The main agent is the orchestrator and the only actor allowed to mutate shared external state: Jira comments/statuses, Bitbucket pushes, PR creation/update/reviewer assignment, automation memory, and live report refresh. Subagents are workers, not owners.\n- At run start, the main agent must acquire a single automation run lock for this automation. If another run is active, do not start remediation; report the active run and exit or wait only when explicitly appropriate. Release the lock only after final memory/report reconciliation.\n- At the start of each run, the main agent must write a short delegation plan before spawning workers: current phase, blocked/pending/new-work queues, which tasks stay with the main agent, which subagents are spawned, each subagent's `Name (Role: scope)`, read/write permissions, and browser/API ownership.\n- Keep the immediate critical path local. Delegate only bounded sidecar work that can run independently while the main agent continues orchestration, evidence review, or non-overlapping setup.\n- Give every subagent a frozen memory snapshot, exact scope, expected output format, allowed commands/systems, and explicit stop conditions. Subagents must not re-enumerate and start unrelated work.\n- When spawning, tracking, or reporting a subagent, always write the display name followed by its role in parentheses, using `Name (Role)` or `Name (Role: scope)` format; examples: `Copernicus (Blocker Recheck Agent: PRE-4299)`, `Ada (Repository Fix Agent: enrg-homecenter)`, `Noether (Staging Smoke Agent: PRE-1234)`. Use this format in progress updates, handoffs, memory summaries, live-report notes, and final output so Thien Nguyen can immediately see who did what.\n- Run read-only agents in parallel when possible: PR/Jira Evidence Agents poll PR/Jira/reviewer/approval state via API where possible; Blocker Recheck Agents validate assigned blocked items with fresh command/API evidence only; Dashboard Extraction Agents normalize package-audit details into repository/branch/file/package/severity/advisory findings; Report QA Agents check live report data semantics and UI health.\n- Run browser-backed agents serially under the browser mutex: package-audit extraction, Jira UI fallback, PR page fallback, and staging smoke tests must never overlap. Prefer API calls for read-only PR/Jira polling.\n- The main agent grants a browser lease to exactly one agent at a time. The task brief must name the browser holder and scope, and the handoff must state that the browser lease is released.\n- Repository Fix Agent scope is exactly one repository, one issue, one isolated worktree/security branch, and one disjoint file set. It may prepare and verify a local fix, but must not push, create/update PRs, change Jira, write automation memory, or run report refresh. The main agent reviews the diff and performs commit/push/PR/Jira work.\n- Staging Smoke Agent scope is exactly one assigned PRE/repo/URL area. It may use the browser only when the main agent grants the mutex and may write only the assigned screenshot path; it must not edit memory, JSON, Jira, Bitbucket, or report data. If login/route/test-data is missing, it reports the exact blocker instead of guessing.\n- Every subagent handoff must include: `Name (Role: scope)`, assigned scope, files/repos inspected or changed, commands/API calls run, evidence paths or URLs, result, confidence, remaining blockers, blocker reason if any, and the exact next action recommended for the main agent.\n- Subagents must not print or persist secret values, load credential files, or receive raw tokens. Authenticated read-only API work may only use commands/environments prepared by the main agent, with secrets never printed, persisted, copied into prompts, or copied into handoffs.\n- Avoid write conflicts: no two agents may write the same repo/worktree/branch/file, Jira issue, memory file, report file, or evidence screenshot path at the same time. If scopes collide, stop and let the main agent reassign.\n- Blocker Recheck Agents return fresh evidence only. The main agent decides whether to append Jira comments, move tickets, update memory, or declare the blocker resolved.\n- The main agent must review subagent results before pushing, creating/updating PRs, changing Jira status, writing memory, refreshing the live report, or declaring a blocker resolved.\n- At final reconciliation, the main agent dedupes all subagent handoffs, resolves conflicts, appends a single normalized memory summary, then runs `update_history_report.sh` once and `ensure_live_report_service.sh` once.\n\nUse the Codex in-app browser/current browser session (do not use Google Chrome for the package-audit dashboard and do not spawn unmanaged parallel Codex tasks outside the delegation plan below). Open or reuse the current in-app browser tab for https://sec.check24.de/package-audit?slug=power&vulnerabilityCountsSearch=critical%2Chigh%2Cmedium%2Clow. From package-audit, iterate each issue and open Details to extract repository, branch, filepath, package, severity, and advisory identifiers. Load runtime credentials from /Users/thien.nguyen/enrg-web-frontend/.agents/cli/.env; never print or persist secret values. Use Bitbucket BASIC auth with EMAIL:BB_API_TOKEN and workspace BB_WORKSPACE. For each issue repo in the check24 workspace, resolve slug via workspace API when needed, then use an isolated worktree under the automation workspace or clone if absent. Before changes, always fetch, reset to latest scanned target branch, and rebase security branch onto latest target before push. Before any force-push, the main agent must fetch the remote branch and verify it contains only automation-owned commits or matches the expected previous automation head; if human or unknown commits are present, stop and report a blocker. Apply fixes as before: npm/pnpm lockfile vulnerabilities via package manager with focused lockfile diffs; composer vulnerabilities via targeted composer update of affected package with dependencies, no install, no scripts, and ignore platform requirements when needed, using Nexus credentials from env. For lockfiles, preserve unrelated metadata and dependency nodes (including peer metadata like \"peer\": true); if package-manager regeneration introduces unrelated churn (e.g. peer-flag flips, optional platform binaries, broad resolver noise), rebuild lockfile from parent and patch only vulnerable package nodes, then amend the existing commit and force-push the same security branch. Verify each fix with the same scanner/command family that reported it (npm audit/pnpm audit/composer audit/osv-scanner as applicable). Commit each changed repo on a concise 00000-style security branch, push, create or reuse Bitbucket PR to the scanned target branch, and include a proper PR description covering: what changed, side effects, breakage risk, rollback notes, post-merge checks, and lockfile-diff reasoning from actual committed changes (not local-environment assumptions). Always add Jens Bachmann and Aleksandar Jaksic as reviewers while preserving existing reviewers; resolve user UUIDs by display name if needed. If the scanned target branch is still vulnerable, ensure an open PR exists from the security branch where the same file resolves to a patched version; create or update PR if missing.\n\nStaging smoke-test evidence workflow (mandatory when a fix is pushed to staging):\n- Staging URL is https://www.check24-test.de/strom/. After a fix branch/PR is pushed to staging or otherwise made visible on staging, infer the relevant staging area from the changed repository, route, component, package usage, and code ownership.\n- If the relevant area is visible without missing credentials, smoke test that area in the browser, take a screenshot, and save it under `/Users/thien.nguyen/.codex/automations/fix-slack-security-dependency-threads/history-dashboard/evidence/<YYYY-MM-DD>/<ticket-or-repo>-staging-smoke.png`.\n- Record the staging URL tested, tested area, reasoning from the code, screenshot path, and pass/fail result in memory and in the final output so the live report can show it as release evidence.\n- If the relevant area cannot be inferred, is not visible on staging, needs login, or requires missing test data, do not pretend the smoke test passed; record the exact blocker/question and ask Thien Nguyen for the missing route/login/test data.\n\nJira workflow (mandatory for every security issue):\n0) Before creating Jira tickets or PRs, the main agent must search memory and Jira/Bitbucket by repo, branch, filepath, package, severity, and advisory ID; reuse existing open work items instead of creating duplicates.\n1) Using Jira API when available or the serialized in-app browser session as fallback, create a Jira ticket in project PRE via https://c24-energie.atlassian.net/jira/software/c/projects/PRE/boards/132.\n2) Set both Assignee and Developer to Thien Nguyen, then move status to In Progress immediately.\n2.1) Immediately verify the created Jira work item is visible on the PRE board and specifically on https://c24-energie.atlassian.net/jira/software/c/projects/PRE/boards/132?assignee=712020%3A98c2de13-71c4-48ae-a98a-3baa7fa11ba2. If not visible, adjust board-relevant fields (status, assignee, labels/epic) and re-rank so it appears in that filtered board view, then record what was changed.\n3) After PR exists, update Jira with PR link and move status to In Code Review.\n4) Assign reviewer/owner in code review context to Alek (Aleksandar Jaksic).\n5) Poll PR approval status during each daily run (Bitbucket API preferred; serialized in-app browser PR page fallback). When approved, move Jira ticket to Approved and notify Thien Nguyen in the run output.\n6) If an item is blocked, add a Jira ticket comment containing blocker reason/root cause/evidence/next action, then move ticket to To Do (never leave blocked tickets in Open).\n7) On later runs, re-check each blocked To Do ticket first; if still blocked, append a fresh blocker comment; if no longer blocked, move ticket to In Progress and continue processing.\n\nBlocked item recheck workflow (mandatory first phase of every run):\n- Before scanning the dashboard, creating new tickets, or modifying repositories for new findings, read memory and enumerate all blocked Jira tickets and blocked memory items.\n- For each blocked item, re-run the concrete blocker check: registry version lookup, npm/pnpm/composer/osv audit, Bitbucket PR/API check, Jira status check, dashboard detail check, or the exact command/API family recorded in memory.\n- If the item is still blocked, append a fresh Jira comment with the new timestamp, root cause, concrete evidence/error text, and recommended next action, keep or move the ticket to To Do, verify board visibility when Jira is reachable, and save the same evidence in memory.\n- If the blocker is gone, move the Jira ticket from To Do to In Progress, continue the fix/PR workflow immediately, and record the unblocked evidence in memory and output.\n- If Jira or the required external service is unreachable, treat that access failure as the fresh blocker evidence for this run and still include the Jira URL in memory and output.\n- Never skip a blocked item silently. The final output must include a recheck result for every blocked item from memory.\n\nFor every blocked item, always write an explicit blocker reason with root cause, concrete evidence (error text/command result), and recommended next action. If a Jira ticket exists for that blocked item, add the same blocker reason as a Jira ticket comment and move it to To Do (not Open). Always include the Jira ticket URL for each blocked item in the blocked-status section of the run output and saved memory. Track blocked tickets in memory so they are re-checked on the next run.\n\nLive report workflow:\n- At the end of every run, after writing the memory summary, regenerate the live report data by running `/Users/thien.nguyen/.codex/automations/fix-slack-security-dependency-threads/history-dashboard/update_history_report.sh`, then run `/Users/thien.nguyen/.codex/automations/fix-slack-security-dependency-threads/history-dashboard/ensure_live_report_service.sh` again so the report is still on after the run completes.\n- The live report server is kept running by the LaunchAgent `com.codex.security-live-report`. Check/recover it with `/Users/thien.nguyen/.codex/automations/fix-slack-security-dependency-threads/history-dashboard/ensure_live_report_service.sh`; install or force-restart it with `/Users/thien.nguyen/.codex/automations/fix-slack-security-dependency-threads/history-dashboard/install_live_report_service.sh`; direct foreground start is still available with `start_live_report.sh`. It serves `http://127.0.0.1:8765/` and pushes updates over `ws://127.0.0.1:8765/ws`.\n- The live report data source is `/Users/thien.nguyen/.codex/automations/fix-slack-security-dependency-threads/history-dashboard/security_audit_history.json`. If the live server is running, JSON changes must be pushed to connected browsers by WebSocket automatically.\n- Do not generate, recreate, or present `security_audit_history.html`; the report is live-only and backed by `security_audit_history.json`, `http://127.0.0.1:8765/`, and `ws://127.0.0.1:8765/ws`.\n- Include the live report URL `http://127.0.0.1:8765/` in the final run output instead of presenting any static HTML file.\n- If report data generation or live server startup fails, include the command error as a blocker, but do not discard the memory summary.\n\nAt the end, clearly report repos already fixed, repos changed, verification performed, pushed branches, PR links, reviewer assignment results, Jira ticket links/status transitions (including To Do/In Progress/Approved moves), ticket visibility verification results (including the assignee-filtered board URL), approval polling results, notifications sent, live report URL, WebSocket endpoint, report data path, and blockers/auth failures. In the blocked section, include one clickable Jira link per blocked item and the fresh recheck result for that run."
status = "ACTIVE"
rrule = "RRULE:FREQ=WEEKLY;BYHOUR=8;BYMINUTE=0;BYDAY=SU,MO,TU,WE,TH,FR,SA"
model = "gpt-5.5"
reasoning_effort = "high"
execution_environment = "local"
cwds = ["/Users/thien.nguyen/Documents/Codex/2026-05-06/check-open-thread-on-slack-https"]
created_at = 1778053386681
updated_at = 1779783509579
