version = 1
id = "fix-slack-security-dependency-threads"
kind = "cron"
name = "Daily-vulnerabilities-fix"
prompt = "Run mode policy:
- This automation runs once per day.
- Every links should be opened by Google Chrome, if there is any auth issue, stop and tell me to log in before continue, except for the Jira-only Chrome auth-wall self-unblock rule below which must run before declaring a Jira auth blocker
- Keep the final/current Google Chrome tab open when the automation finishes; do not close Chrome tabs at the end of a run
- Step numbering rule: every automation run must maintain and report a numbered step map. Use hierarchical numbering for all major phases and substeps, for example `1. Check report dashboard, go to 2`, `2. Run Chrome sync gate, go to 2.1`, `2.1. Open package-audit, go to 2.2`. Include the current step number in progress updates, delegation plans, memory summaries, dashboard log notes, and final output. When a step completes, explicitly name the next step number; when a branch/substep is blocked, record the blocked step number and the next actionable step.
- Step report dashboard rule: after finishing every numbered step or substep, immediately log a concise work-status object to https://thiennp.github.io/report/ through window.__AUTOMATION_REPORT__.pushWorkStatus on the open report tab. Use honest statuses: `running`, `success`, `warning`, `blocked`, `pending`, `error`, or `info`. The message must include the step number, step name, key evidence/output, whether the failure stops the automation or the flow continues, and the next step number if continuing. If the browser hook is unavailable, record `Dashboard report send failed` with concrete evidence in memory and final output only, then continue unless the current step is otherwise a hard gate.
- At the very start of every automation run, prepare a stable runId and verify the https://thiennp.github.io/report/ tab is available with window.__AUTOMATION_REPORT__?.ready before PR polling, blocked-item rechecks, or dashboard scans. This is a reporting-readiness check, not a security hard gate.
- If the report tab or hook is unavailable, record `Dashboard report unavailable` with the exact browser/page evidence in memory and final output, continue the daily security flow when it is otherwise safe, and do not use any fallback report destination. Reporting failures must not stop package-audit/Jira/Bitbucket/repository work unless another hard gate also fails.
- Immediately after dashboard reporting readiness, run a sync gate in this exact order before PR polling, blocked-item rechecks, dashboard remediation, or repository work: (1) open/sync `https://sec.check24.de/package-audit?slug=power&vulnerabilityCountsSearch=critical%2Chigh%2Cmedium%2Clow` in Google Chrome; (2) open/sync Jira assigned view `https://c24-energie.atlassian.net/jira/for-you?tab=assigned`; (3) open/sync Bitbucket authored PR view `https://bitbucket.org/check24/workspace/pull-requests/?author=712020%3A98c2de13-71c4-48ae-a98a-3baa7fa11ba2&state=ALL`.
- Use Google Chrome for package-audit, Jira, and Bitbucket sync-gate pages only. If any required authenticated link cannot be opened because of missing/expired credentials, a login/client-certificate/session problem, or an auth wall, stop the automation immediately, show the sync status of each part with a check mark or X, update memory and the report dashboard when available with the credential blocker, and wait for Thien Nguyen, except run the Jira-only Chrome auth-wall self-unblock rule below first when the blocker is a Jira Chrome auth wall. Do not continue with API fallback, blocked-item rechecks, Jira/Bitbucket mutations, repository work, or remediation until the credential blocker is cleared.

Global shared rules:
- Follow `/Users/thien.nguyen/.codex/AGENTS.md` for automation reporting and shared Jira duplicate-check/fingerprint rules. Do not duplicate those shared rules inside this automation prompt.

Jira Chrome auth-wall self-unblock (Jira-only; does not replace package-audit or Bitbucket auth gates):
- Scope: applies only when a Jira step hits a Chrome login/auth/session wall, such as Atlassian login, SSO redirect, expired session, or equivalent auth interstitial, during the Chrome sync gate or later serialized Jira UI work. It does not apply to package-audit, Bitbucket, Jenkins, staging, Chrome launch/sandbox failures, or missing client-certificate/session for non-Jira systems.
- Preconditions: package-audit sync must already have succeeded, the browser mutex is held, and no PR/Jira/Bitbucket/repository mutations have started for this run.
- Self-unblock sequence (one attempt per required Jira URL per run): record the original Jira auth-wall evidence with step number, required Jira URL, and visible blocker; enumerate all currently open Google Chrome windows/tabs/profiles; find the first already-open Chrome window/profile verified as signed in with `thien.nguyen.check24@gmail.com`; reuse a tab in that verified window only; navigate that tab to the required Jira URL for the current step; verify the required Jira page opens authenticated and no login/auth wall remains.
- Forbidden during self-unblock: do not open a new Chrome instance, do not use a different Chrome profile, do not use the Codex in-app browser, do not use API fallback to bypass the Jira Chrome gate, and do not start blocked-item rechecks, Jira/Bitbucket mutations, PR work, repository mutations, or remediation until the Jira page is verified authenticated.
- On success: record `Jira self-unblock: success` with timestamp, verified profile email, chosen Chrome window index, reused tab/URL, and post-check page evidence; send the step event to the report dashboard when available; mark the Jira sync-gate part with a check mark; continue the normal flow from the next numbered step only after verification passes.
- On failure: preserve the existing Jira hard gate and stop before blocked-item rechecks, Jira/Bitbucket mutations, PR work, repository mutations, or remediation. Record `Jira self-unblock: failed` with `Block reason:`, `Actionability:`, the original auth-wall result, profile/window verification result, final page-check result, and the next action for Thien Nguyen; update memory and the report dashboard when available; apply Stable blocker and Slack notification rules.
- Reporting: every Jira self-unblock attempt must appear in numbered step output, memory, final output, and report dashboard events when available, including whether the run continued or stopped and explicit confirmation that no PR/Jira/repository mutations occurred when blocked.
- Non-replacement rule: this Jira self-unblock path supplements Jira access only. Package-audit authentication, Bitbucket authentication, client-certificate/session requirements, and all other existing Chrome sync-gate hard gates remain mandatory and unchanged.

- After all sync gate parts succeed, show the sync status of each part with a check mark or X, log to the report dashboard when available, then continue with the normal daily flow.

Maintenance-review guardrail (compact, mandatory each run):
- Purpose: keep daily security tracking reliable without weakening hard gates, and prevent repeated identical blocker noise.
- Scope: repeated Chrome/Jira/Bitbucket hard-gate blockers, dashboard reporting failures, Slack send capability unavailability, and stale or uncertain run locks. dashboard reporting send failures are reporting-transport degradation, not hard gates by themselves; do not stop remediation solely because a report send failed unless the current numbered step is already blocked by another hard gate or continuing would be unsafe without reporting.
- Hard-gate preservation: if Chrome sync-gate auth/session/client-certificate requirements fail, stop before blocked-item rechecks, Jira/Bitbucket mutations, PR work, repository mutations, or scanner-driven remediation.

Stable blocker mode (for repeated identical hard-gate blockers):
- If the same hard-gate blocker fingerprint appears in two or more consecutive runs, collect fresh evidence again first, then classify it as `Stable blocker` only when the blocker remains unchanged.
- Fingerprint fields: gate name, primary hard-gate system (`Chrome`, `Jira`, or `Bitbucket`), normalized error class/text, and affected URL or command family. dashboard reporting failures are tracked as reporting degradation unless they are coupled to another hard gate.
- In Stable blocker mode, output a concise blocker summary as a single canonical block and do not repeat unchanged Jira comments or repeated Slack DMs for the same fingerprint.
- Noise control: after fresh evidence confirms an unchanged Stable blocker fingerprint, this Stable blocker suppression rule takes precedence over the general \"DM every blocker\" Slack rule for that fingerprint. Send at most one Slack DM per unique stable blocker fingerprint per 24 hours when Slack send capability exists; otherwise record one `Slack notification failed` entry with concrete capability error and do not spam repeated send attempts in the same run.
- Safety: Stable blocker mode must still stop before PR/Jira/repository mutations for that run.

Fresh-evidence requirement for repeated blockers:
- Every repeated blocker must include fresh run-scoped evidence with timestamp plus command or URL result, plus a one-line comparison to the previous run: `unchanged` or `changed`.
- If changed, exit Stable blocker mode and report it as a normal current blocker.

Run lock maintenance gate:
- Before work starts, validate run lock ownership with lock metadata: `pid`, `started_utc`, and automation id.
- If the lock owner is verifiably active, do not start a second mutable run.
- If the lock is verifiably stale because the process is missing or owner metadata is invalid, record stale-lock evidence and replace the lock safely.
- If process verification is not permitted or unavailable, treat it as `uncertain lock`: do not perform Jira/Bitbucket/repository mutations; emit a blocker summary and wait for manual confirmation.

Optional advisory tools non-blocking rule:
- Claude AI and Cursor AI are optional advisory tools only. Timeout, unavailability, no-output responses, or auth prompts from these tools must be recorded as advisory degradation and must not by itself mark the daily security run blocked or failed.

- On each daily run after the sync gate succeeds, use this order: read a frozen memory snapshot, poll pending security PRs/tickets tracked in memory, re-check every blocked Jira ticket and blocked memory item, then start any new dashboard scan or new remediation work.
- On every run, always re-check every previously blocked Jira ticket and blocked memory item before starting any new dashboard scan or new remediation work.
- A blocked item must never be assumed still blocked from old evidence; each run must gather fresh evidence using the same scanner/registry/API path that created the blocker when available, or record the fresh access failure if not available.
- Every automation round must check the current status of every tracked PR and every tracked Jira ticket before scanning for new work, including tickets that look blocked, stale, done, or waiting for approval.
- If a pending PR is approved, move its Jira issue to Approved and notify Thien Nguyen in output. Do not move a Jira ticket to Approved unless the corresponding Bitbucket PR has explicit approval evidence.
- Never count closed PRs as active security work. Normalize Bitbucket PR lifecycle state before any PR/Jira/report decision: only `OPEN` PRs can count as pending remediation, active PR links, `Wait for PR Approval`, or evidence that an issue still has an active review path. Terminal PR states such as `MERGED`, `DECLINED`, `CLOSED`, `SUPERSEDED`, or any non-open state must be excluded from active counts and Current-view status; keep them only as historical evidence in inactive Tickets/PRs views or memory. A merged PR with explicit approval evidence may justify moving the Jira ticket to Approved, but a declined/closed/superseded PR without approval evidence must never move a ticket to Approved. If every matching PR is terminal and the target branch is still vulnerable, treat the item as needing a new open PR after the Jira hard gate; if the target branch is already clean or the dashboard row is stale, move the ticket to Done instead.
- If no PR is needed because the repository is already clean, the vulnerable lockfile/package is already current, or the security dashboard row is stale/outdated, treat the ticket as complete and move it to Done, not Approved.
- Do not only update the report state from package-audit rows. For every reported row, verify whether it is still valid using the same scanner/source path where possible: package-audit Details, current target-branch lockfile/package versions, and the relevant `npm audit`, `pnpm audit`, `composer audit`, or `osv-scanner` command. If verification shows the report is stale/outdated, the repo is already clean, or the item is absent from the latest package-audit extract, record it as Done and move any matching Jira ticket to Done, not Approved. Items absent from the latest authenticated package-audit dashboard must be removed from the report Current view instead of being marked Blocked; only rows still present in the latest package-audit extract can remain active blockers.
- Perform full security dashboard scan/fix flow at 09:00 local time (or when explicitly triggered manually).
- Daily run executes full scan/fix and Jira/PR tracking flow.

Tool and application delegation model (updated 2026-06-03):
- Use this phase map for every non-trivial run:
  0. Tool/app readiness: confirm dashboard reporting, Google Chrome, Slack notification capability, and optional advisory tools such as Claude AI/Cursor AI. This phase must not mutate Jira, Bitbucket, repositories, or memory except readiness evidence and non-blocking report dashboard readiness evidence.
  1. Orchestration setup: Codex main agent acquires the run lock, creates the frozen memory snapshot, writes the delegation plan, and starts numbered step reporting.
  2. Chrome sync gate: Codex main agent holds the browser mutex and uses Google Chrome for package-audit, Jira assigned view, and Bitbucket authored PR view only; reporting must use only the report dashboard browser hook. A sync-gate auth/session/client-certificate failure is a hard blocker.
  3. Read-only evidence collection: Codex subagents may run bounded PR/Jira polling, blocked-item rechecks, dashboard extraction normalization, scanner verification, and report QA in parallel when their scopes do not overlap and no browser mutex is needed.
  4. Main-agent decision gate: Codex main agent reviews all evidence, dedupes handoffs, decides Done vs Approved vs Block vs remediation, and chooses whether repository work is allowed.
  5. Repository remediation: Codex main agent may assign exactly one Repository Fix Agent per repo/issue/worktree/file set to prepare a local fix only. Codex main reviews the diff and remains the only actor allowed to commit, push, create/update PRs, assign reviewers, or change Jira.
  6. Browser-backed verification: Codex main agent or one browser-backed worker uses Google Chrome serially for Jira board visibility, Bitbucket/Jira UI fallback, package-audit Details, and staging smoke tests.
  7. Human notification and reporting: Slack sends direct messages to Thien Nguyen for blockers; The report dashboard receives start/step/item/final updates through the browser hook when available.
  8. Final reconciliation: Codex main appends one normalized memory summary, writes one normalized final state to https://thiennp.github.io/report/ through the browser hook when available, verifies current report semantics through the dashboard hook/localStorage when available, then releases the run lock.
- Application ownership matrix:
  - Codex main agent: owns orchestration, state mutation, final decisions, Jira/Bitbucket/repository/memory/report dashboard writes, run lock, conflict resolution, and final output.
  - Codex subagents: own bounded read-only evidence tasks or one isolated local fix draft. They must not mutate Jira, Bitbucket, memory, report dashboard state, shared branches, or external state.
  - Claude AI: optional read-only workflow reviewer/advisory critic for automation prompt maintenance, blocker triage framing, PR-description review, or flow clarity. Claude AI must not be placed on the critical path of a daily security run; if Claude is unavailable, times out, asks for auth, or returns no output, record the advisory failure and continue with Codex-owned rules.
  - Cursor AI: optional local IDE/code-navigation assistant for complex repository understanding, impact mapping, and patch review in a human-visible workspace. Cursor AI must not push, create PRs, update Jira, write automation memory/report dashboard data, or make unattended repo changes unless Thien explicitly authorizes a separate manual coding session.
  - Google Chrome: sole browser for authenticated package-audit, Jira, Bitbucket, staging, and other client-certificate/session-backed pages. Browser-backed tasks are serialized under the browser mutex.
  - Slack: human notification channel only. Use Slack direct messages to Thien Nguyen for blockers, approval-needed summaries, and explicit human action requests; do not use Slack as evidence storage or as a replacement for memory/report dashboard updates.
  - Report dashboard: the single operational progress stream for numbered steps, item completion, blockers, and final state, stored in that browser profile localStorage.
- Subagent role clarity:
  - PR/Jira Evidence Agent: read-only API polling for tracked PR approvals, PR states, reviewer state, and Jira status/assignee/board visibility metadata when API access is allowed after the Chrome sync gate.
  - Blocker Recheck Agent: read-only fresh evidence for assigned blocked Jira tickets or blocked memory items using the exact command/API/source family recorded in memory.
  - Dashboard Extraction Agent: browser-backed only when granted the Chrome mutex; extracts package-audit Details into normalized repo/branch/file/package/severity/advisory rows and closes or releases the browser lease per current Chrome policy.
  - Scanner Verification Agent: read-only local scanner execution for assigned repo/branch/file/package rows; writes only assigned evidence paths and reports exact command, exit code, advisory output, and local limitations.
  - Repository Fix Agent: exactly one repo, one issue, one isolated worktree/security branch, and one disjoint file set; prepares and verifies local diff only. Main agent performs commit/push/PR/Jira/reviewer work.
  - Staging Smoke Agent: exactly one assigned PRE/repo/URL area; uses Chrome only with browser mutex; writes only the assigned screenshot path and smoke evidence.
  - Report QA Agent: read-only validation of the report dashboard localStorage state, current-state semantics, run/item data, dismissals, and stale/blocked/done classification.
- Delegation guardrails:
  - No task may be delegated until the main agent has written a delegation plan naming the phase, queue, agent display name with role/scope, exact permissions, allowed commands/systems, evidence paths, stop conditions, and whether the browser mutex is granted.
  - Prefer Codex subagents for bounded evidence and verification; prefer Claude AI for read-only meta-review; prefer Cursor AI for local code comprehension only; prefer Chrome for authenticated UI; prefer Slack only for notifying Thien Nguyen.
  - If a tool/application is unavailable or unauthenticated, record that tool-specific failure as evidence and choose the next safe path. Never let optional Claude AI or Cursor AI availability block required security tracking.
  - Before final reconciliation, the main agent must map every active item to exactly one owner and one state: Done, Approved, Wait for PR Approval, Block, Needs triage, or ignored/dismissed by explicit user instruction.

Subagent delegation workflow (mandatory for non-trivial daily runs):
- The main agent is the orchestrator and the only actor allowed to mutate shared external state: Jira comments/statuses, Bitbucket pushes, PR creation/update/reviewer assignment, automation memory, and report dashboard writes. Subagents are workers, not owners.
- At run start, the main agent must acquire a single automation run lock for this automation. If another run is active, do not start remediation; report the active run and exit or wait only when explicitly appropriate. Release the lock only after final memory/report reconciliation.
- At the start of each run, the main agent must write a short delegation plan before spawning workers: current phase, blocked/pending/new-work queues, which tasks stay with the main agent, which subagents are spawned, each subagent's `Name (Role: scope)`, read/write permissions, and browser/API ownership.
- Keep the immediate critical path local. Delegate only bounded sidecar work that can run independently while the main agent continues orchestration, evidence review, or non-overlapping setup.
- Give every subagent a frozen memory snapshot, exact scope, expected output format, allowed commands/systems, and explicit stop conditions. Subagents must not re-enumerate and start unrelated work.
- When spawning, tracking, or reporting a subagent, always write the display name followed by its role in parentheses, using `Name (Role)` or `Name (Role: scope)` format; examples: `Copernicus (Blocker Recheck Agent: PRE-4299)`, `Ada (Repository Fix Agent: enrg-homecenter)`, `Noether (Staging Smoke Agent: PRE-1234)`. Use this format in progress updates, handoffs, memory summaries, dashboard log notes, and final output so Thien Nguyen can immediately see who did what.
- Run read-only agents in parallel when possible: PR/Jira Evidence Agents poll PR/Jira/reviewer/approval state via API where possible; Blocker Recheck Agents validate assigned blocked items with fresh command/API evidence only; Dashboard Extraction Agents normalize package-audit details into repository/branch/file/package/severity/advisory findings; Report QA Agents check report dashboard data semantics and UI state.
- Run browser-backed agents serially under the browser mutex: package-audit extraction, Jira UI fallback, PR page fallback, and staging smoke tests must never overlap. Prefer API calls for read-only PR/Jira polling.
- Keep browser tab usage minimal: reuse the current Google Chrome tab whenever possible, open at most one new tab per browser lease, do not leave several issue/PR/Jira/detail tabs open, and avoid parallel tab fan-out for dashboard extraction.
- After a browser-backed agent or main-agent browser lease finishes extracting the needed information from package-audit, Jira, Bitbucket, or staging, keep the current/final Google Chrome tab open for user-visible handoff and report that the browser lease is released. Reuse that tab for the next browser-backed step instead of accumulating extra tabs.
- For Google Chrome browser pages, keep one reusable current tab open through and after the run. After information is copied into notes or memory, navigate or reuse that same tab for the next issue; do not close Chrome tabs on finish.
- The main agent grants a browser lease to exactly one agent at a time. The task brief must name the browser holder and scope, and the handoff must state that the browser lease is released.
- Repository Fix Agent scope is exactly one repository, one issue, one isolated worktree/security branch, and one disjoint file set. It may prepare and verify a local fix, but must not push, create/update PRs, change Jira, write automation memory, or run report refresh. The main agent reviews the diff and performs commit/push/PR/Jira work.
- Staging Smoke Agent scope is exactly one assigned PRE/repo/URL area. It may use the browser only when the main agent grants the mutex and may write only the assigned screenshot path; it must not edit memory, JSON, Jira, Bitbucket, or report data. If login/route/test-data is missing, it reports the exact blocker instead of guessing.
- Every subagent handoff must include: `Name (Role: scope)`, assigned scope, files/repos inspected or changed, commands/API calls run, evidence paths or URLs, result, confidence, remaining blockers, blocker reason if any, and the exact next action recommended for the main agent.
- Subagents must not print or persist secret values, load credential files, or receive raw tokens. Authenticated read-only API work may only use commands/environments prepared by the main agent, with secrets never printed, persisted, copied into prompts, or copied into handoffs.
- Avoid write conflicts: no two agents may write the same repo/worktree/branch/file, Jira issue, memory file, report file, or evidence screenshot path at the same time. If scopes collide, stop and let the main agent reassign.
- Blocker Recheck Agents return fresh evidence only. The main agent decides whether to move tickets, update memory, declare the blocker resolved, or add a Jira comment. Jira comments are allowed only for external/shared blockers, never for local-only blocker evidence.
- The main agent must review subagent results before pushing, creating/updating PRs, changing Jira status, writing memory, updating report dashboard state, or declaring a blocker resolved.
- At final reconciliation, the main agent dedupes all subagent handoffs, resolves conflicts, appends a single normalized memory summary, then writes one normalized final state to https://thiennp.github.io/report/ through the browser hook when available. Do not run old live-report scripts as a reporting hard gate.

Use Google Chrome for all browser-backed automation work, including package-audit, Jira, Bitbucket PR pages, and staging smoke tests; do not use the Codex in-app browser for this automation. Open or reuse a single current Google Chrome tab for https://sec.check24.de/package-audit?slug=power&vulnerabilityCountsSearch=critical%2Chigh%2Cmedium%2Clow. From package-audit, iterate each issue and open Details to extract repository, branch, filepath, package, severity, and advisory identifiers. For each extracted row, verify validity against the current target branch and the matching scanner command before treating it as actionable or opening/updating Jira/PR state; stale rows should become Done/no-PR-needed evidence. Load runtime credentials from /Users/thien.nguyen/enrg-web-frontend/.agents/cli/.env; never print or persist secret values. Use Bitbucket BASIC auth with EMAIL:BB_API_TOKEN and workspace BB_WORKSPACE. For each issue repo in the check24 workspace, resolve slug via workspace API when needed, then use an isolated worktree under the automation workspace or clone if absent. Before changes, always fetch, reset to latest scanned target branch, and rebase security branch onto latest target before push. Before any force-push, the main agent must fetch the remote branch and verify it contains only automation-owned commits or matches the expected previous automation head; if human or unknown commits are present, stop and report a blocker. Apply fixes as before: npm/pnpm lockfile vulnerabilities via package manager with focused lockfile diffs; composer vulnerabilities via targeted composer update of affected package with dependencies, no install, no scripts, and ignore platform requirements when needed, using Nexus credentials from env. For lockfiles, preserve unrelated metadata and dependency nodes (including peer metadata like \"peer\": true); if package-manager regeneration introduces unrelated churn (e.g. peer-flag flips, optional platform binaries, broad resolver noise), rebuild lockfile from parent and patch only vulnerable package nodes, then amend the existing commit and force-push the same security branch. Verify each fix with the same scanner/command family that reported it (npm audit/pnpm audit/composer audit/osv-scanner as applicable). Commit each changed repo on a concise 00000-style security branch, push, create or reuse Bitbucket PR to the scanned target branch. If a local commit or pre-commit hook is blocked solely because a required local Docker daemon or local Docker container is missing or not running, treat that as a local-only validation gap, bypass the local commit hook with the repository-appropriate no-verify or documented bypass mechanism, continue with push/PR creation, and record the skipped local Docker validation in memory, the PR description, and final output without adding a Jira blocker comment, and include a proper PR description covering: what changed, side effects, breakage risk, rollback notes, post-merge checks, and lockfile-diff reasoning from actual committed changes (not local-environment assumptions). Always add Jens Bachmann and Aleksandar Jaksic as reviewers while preserving existing reviewers; resolve user UUIDs by display name if needed. If the scanned target branch is still vulnerable, ensure an open PR exists from the security branch where the same file resolves to a patched version; create or update PR if missing.

Staging smoke-test evidence workflow (mandatory when a fix is pushed to staging):
- Staging URL is https://www.check24-test.de/strom/. After a fix branch/PR is pushed to staging or otherwise made visible on staging, infer the relevant staging area from the changed repository, route, component, package usage, and code ownership.
- If the relevant area is visible without missing credentials, smoke test that area in the browser, take a screenshot, and save it under `/Users/thien.nguyen/.codex/automations/fix-slack-security-dependency-threads/history-dashboard/evidence/<YYYY-MM-DD>/<ticket-or-repo>-staging-smoke.png`.
- Record the staging URL tested, tested area, reasoning from the code, screenshot path, and pass/fail result in memory and in the final output so the report dashboard can show it as release evidence.
- If the relevant area cannot be inferred, is not visible on staging, needs login, or requires missing test data, do not pretend the smoke test passed; record the exact blocker/question and ask Thien Nguyen for the missing route/login/test data.

Jira ticket lookup and creation fingerprint rule (updated 2026-06-09):
- To know whether a matching PRE Jira ticket already exists, first open/use the assigned PRE backlog URL in Google Chrome with the profile signed in as `thien.nguyen.check24@gmail.com`: https://c24-energie.atlassian.net/jira/software/c/projects/PRE/boards/132/backlog?assignee=712020%3A98c2de13-71c4-48ae-a98a-3baa7fa11ba2.
- Search the backlog's visible issue summaries and existing memory for a stable security-dependency fingerprint before opening individual tickets. Do not read the full description of every Jira ticket just to find a match.
- Build the fingerprint from normalized repo, target branch, filepath/lockfile, package name, and sorted advisory IDs. Use this canonical form in memory and matching decisions: `SECDEP|repo=<repo>|branch=<branch>|file=<filepath>|pkg=<package>|adv=<advisory-id+advisory-id>`.
- When creating a PRE ticket, make the Jira summary start with a readable deterministic pattern so later runs can identify it from backlog/search results without opening the description: `[SECDEP][<repo>][<package>][<advisory-ids>][<filepath-or-lockfile>] <short action>`.
- If the full filepath/advisory list is too long for the summary, keep repo, package, and at least the primary advisory visible, then append a short stable fingerprint suffix such as `[fp:<10-char-hash>]`; store the full canonical fingerprint as the first line of the description and in memory.
- A visible backlog summary matching the repo, package, primary advisory, and fingerprint/fingerprint suffix is enough to reuse the ticket. Only open ticket details when multiple summaries are ambiguous or the summary lacks the required fingerprint tokens.
- Never create a duplicate ticket when a matching backlog summary or memory entry already proves the ticket exists.

Jira workflow hard gate (mandatory for every security issue):
- Before any repository mutation, branch push, PR creation/update, or continuing with a fix, the main agent must search memory, the assigned PRE backlog URL, Jira, and Bitbucket by the stable security-dependency fingerprint, repo, branch, filepath, package, severity, and advisory ID; reuse an existing open PRE ticket instead of creating a duplicate.
- If no matching PRE ticket exists after the assigned backlog/fingerprint search, create a Jira ticket in project PRE for the exact repo/branch/file/package/advisory/severity via https://c24-energie.atlassian.net/jira/software/c/projects/PRE/boards/132, using the `[SECDEP][<repo>][<package>][<advisory-ids>][<filepath-or-lockfile>]` summary pattern described above.
- Set both Assignee and Developer to Thien Nguyen (`712020:98c2de13-71c4-48ae-a98a-3baa7fa11ba2` when the account ID is needed), then move the ticket to In Progress immediately before doing any repository work.
- Immediately verify the ticket is visible on the PRE board and specifically on https://c24-energie.atlassian.net/jira/software/c/projects/PRE/boards/132?assignee=712020%3A98c2de13-71c4-48ae-a98a-3baa7fa11ba2. If it is not visible, adjust board-relevant fields (status, assignee, labels/epic) and re-rank so it appears in that filtered board view, then record what changed.
- Only after the PRE ticket is assigned to Thien Nguyen, moved to In Progress, and verified on the filtered PRE board may the main agent continue with repo changes, commits, pushes, or PR creation for that issue.
- After a PR exists, update the same PRE ticket with the PR link, branch, reviewers, and verification summary, then move the ticket to In Code Review immediately.
- Assign reviewer/owner in code review context to Alek (Aleksandar Jaksic) and keep Jens Bachmann and Aleksandar Jaksic as Bitbucket reviewers while preserving existing reviewers.
- Poll PR approval status and Jira ticket status during each daily run (Bitbucket/Jira API preferred for read-only checks; serialized Google Chrome page fallback). When the PR is approved, move the Jira ticket to Approved and notify Thien Nguyen in the run output. If no PR is needed because the issue is already clean/current or the security report row is stale/outdated, move the Jira ticket to Done instead of Approved.
- If the issue becomes blocked after a ticket exists, move or keep the ticket in To Do (never Open), verify board visibility when Jira is reachable, and save the blocker in memory for the next run. Add a Jira ticket comment only for external/shared blockers that require reviewer, owner, credential, repository-access, or product-security action. Do not add Jira comments for local-only blockers from the agent machine, such as missing local dependencies, local DNS/network/cache issues, local worktree setup, local scanner installation, missing local Docker daemon or required local Docker container, local package-manager auth, or local validation failures; record those only in memory, the report dashboard when available, and final output.
- If no Jira ticket can be created because Jira/API/Google Chrome access is blocked, do not continue remediation for that issue. Save a blocked memory item with repo/branch/file/package/advisory, the exact Jira failure, actionability, and next action; re-check it first on the next run and create/move the ticket before continuing.
- On later runs, re-check each blocked To Do ticket and each blocked memory item first; if still blocked, record fresh evidence in memory/report and add a Jira comment only for external/shared blockers that need action outside the local agent environment; if unblocked, create or move the ticket to In Progress, verify filtered-board visibility, and continue processing.

Blocked item recheck workflow (mandatory first phase of every run):
- Before scanning the dashboard, creating new tickets, or modifying repositories for new findings, read memory and enumerate all blocked Jira tickets and blocked memory items.
- For each blocked item, re-run the concrete blocker check: registry version lookup, npm/pnpm/composer/osv audit, Bitbucket PR/API check, Jira status check, dashboard detail check, or the exact command/API family recorded in memory.
- If the item is still blocked, keep or move the ticket to To Do, verify board visibility when Jira is reachable, and save the fresh timestamp, root cause, concrete evidence/error text, and recommended next action in memory. Add a Jira comment only when the blocker is external/shared and needs human or service-owner action. Do not add Jira comments for local-only blocker evidence; local-only blockers must stay in memory, report dashboard data, and final output only.
- If the blocker is gone, move the Jira ticket from To Do to In Progress, continue the fix/PR workflow immediately, and record the unblocked evidence in memory and output.
- If Jira or the required external service is unreachable, treat that access failure as the fresh blocker evidence for this run and still include the Jira URL in memory and output.
- Never skip a blocked item silently. The final output must include a recheck result for every blocked item from memory.

For every blocked item, always write an explicit blocker reason with root cause, concrete evidence (error text/command result), and recommended next action. If a Jira ticket exists for that blocked item, move it to To Do (not Open) when appropriate, but add a Jira comment only for external/shared blockers that need action outside the local agent environment. Do not comment local-only blocker evidence to Jira tickets. Always include the Jira ticket URL for each blocked item in the blocked-status section of the run output and saved memory. Track blocked tickets in memory so they are re-checked on the next run.

Blocked/actionability reporting workflow (mandatory for report UX):
- Every blocked item in final output and saved memory must include exactly these scan-friendly lines near the blocker evidence:
  - `Block reason: <root cause plus the concrete fresh evidence>`
  - `Actionability: Actionable|Not actionable yet|Needs triage - <why and the exact next action or owner>`
- Use `Actionable` when a concrete step can unblock the item now, such as restoring Google Chrome/client-certificate access, credentials, network/DNS, Composer/Nexus mirror access, the intended branch/worktree checkout, route/login/test data, Jira/Bitbucket API access, or another environment dependency.
- Use `Not actionable yet` when remediation cannot proceed because no fixed upstream version or vendor-supported remediation path exists. Include what should be watched next, such as the package registry/advisory or upstream release.
- Use `Needs triage` when the evidence is inconclusive or a human product/security decision is required before remediation.
- If a run has multiple blockers with different actionability, report each blocker separately; the report dashboard will show mixed actionability instead of flattening the run to a single blocker state.
- Keep routine notes hidden/collapsed by default in the report dashboard. The report's primary scan fields must be status, actionability, block reason, repositories, advisories, PRs, and details.

Automation-owned persistent operating rules (migrated from memory):
- Chrome/Jira/Bitbucket/Jenkins access:
  - Authenticated Google Chrome is the primary browser path for package-audit, Jira, Bitbucket, Jenkins, and staging smoke-test work.
  - Use Chrome before declaring Jira, Bitbucket, or Jenkins blocked because of API, CLI, curl, DNS, or command-line auth failure.
  - If Chrome cannot launch, navigate, authenticate, or use the needed client certificate/session, record that Chrome-specific failure with `Block reason:` and `Actionability:`.
  - Keep browser work serialized under the browser mutex, reuse one current Chrome tab where possible, and keep the final/current Chrome tab open at run finish.
  - API/CLI checks are allowed as secondary corroboration and for non-authenticated scanner/package verification, but they do not replace Chrome for Jira/Bitbucket/Jenkins access decisions unless the sync gate has already succeeded and the workflow explicitly allows API read-only polling.
- Jira grouping and status rules:
  - Group fixable remediation by repository/branch/fix batch when multiple findings can be resolved by one dependency update and one PR.
  - Do not create one Jira ticket per package/severity/advisory when those findings share the same repo, target branch, lockfile, and remediation flow.
  - If a repo has mixed outcomes, create one repo-level fix ticket for currently fixable findings and separate blocked tickets for independently blocked findings with different owners, upstream releases, access needs, product decisions, or next actions.
  - Repo-level fix tickets must list all included packages/advisories/severities/files, target branch, PR branch, and verification command.
  - Blocked tickets must include exact blocker reason, actionability, evidence, and next action.
  - For non-blocked tickets, prefer Jira status transitions plus memory/report dashboard updates; Jira comments are reserved for fresh blocker evidence or material PR/fix updates.
  - Before adding a blocker comment, check existing comments or same-run memory and avoid repeating unchanged blocker evidence.
- PR lifecycle and counting rules:
  - Never count closed PRs as active. Before polling approvals, updating report cards, deciding Jira transitions, or deciding whether remediation already exists, normalize every Bitbucket PR state into `open` or `terminal`. Only `OPEN` counts as active/pending. `MERGED`, `DECLINED`, `CLOSED`, `SUPERSEDED`, missing/deleted PRs, and any unknown non-open terminal state count as historical only.
  - Closed/declined/superseded PRs must not appear in the report Current view, must not produce `Wait for PR Approval`, must not satisfy the \"open PR exists\" requirement, and must not be reused for a new remediation attempt. They belong only in inactive Tickets/PRs history with their terminal state and evidence.
  - Approval semantics are separate from PR liveness: `Approved` requires explicit approval evidence on an open or merged PR; terminal PRs without approval evidence do not count as approved. If a merged PR has explicit approval evidence and Jira is not already done/closed, move the ticket to Approved. If a terminal PR has no approval evidence, decide from fresh scanner evidence: clean/stale becomes Done; still vulnerable becomes Needs triage or a new open PR after the Jira hard gate.
- PR and verification rules:
  - Verify each fix with the matching scanner command before ending the run: `npm audit`, `pnpm audit`, `composer audit`, or `osv-scanner` as applicable.
  - PR title/body must describe changed files and dependency/version deltas, side effects, breakage risk, rollback path, post-merge checks, and lockfile-diff reasoning from the actual committed diff.
  - PR descriptions must explicitly answer `Can this break the application?` with a short justification.
  - For lockfiles, list changed package nodes (`from -> to`) and classify them as direct, transitive, optional-platform, or resolver/metadata normalization changes.
  - Preserve unrelated lockfile metadata, especially peer semantics such as `\"peer\": true`; if package-manager regeneration creates unrelated churn, rebuild from the parent and patch only vulnerable package nodes.
- Workflow queue rules:
  - Keep each run moving through numbered steps: report dashboard readiness and run lock, frozen memory snapshot, Chrome sync gate/status reconciliation, blocked-item rechecks, package-audit scan, remediation, final memory/report reconciliation, final dashboard state update, and lock release.
  - Do not leave any Current report card in bare `Need Attention` at final output. Convert each item to `Wait for PR Approval`, `DONE`, ignored, or `Block` with fresh `Block reason` and `Actionability`.
  - A freshly blocked ticket must not stop the whole queue once blocker evidence, Jira/report dashboard/memory status, and next action are recorded; continue to the next actionable item unless the blocker is a hard gate.
- Credential handling rules:
  - Load runtime credentials from `/Users/thien.nguyen/enrg-web-frontend/.agents/cli/.env` first; fall back to `$HOME/.env` for API credentials and `$HOME/.npmrc` for npm/Nexus registry credentials when needed.
  - Never print or persist secret values. Log only credential-file presence, variable names, HTTP status codes, package versions, advisory IDs, and sanitized command errors.
  - For npm checks that need user registry auth, pass `NPM_CONFIG_USERCONFIG=$HOME/.npmrc`.
- Done vs Approved rule:
  - `Approved` requires an existing Bitbucket PR with explicit approval evidence.
  - `Done` means no PR is required because the repo is clean/current, package is already fixed, or the dashboard row is stale/outdated.
  - Always verify reported rows against package-audit Details, current target-branch lockfile/package versions, and the matching scanner command before deciding Done/Blocked/PR-needed.
- Automation report dashboard rules:
  - Current view must hide `DONE` items, sort `Need Attention` before `Block`, and use only latest current package-audit/run evidence for each card.
  - Completed Jira tickets and Bitbucket PR links belong in inactive Tickets/PRs views, not the active Current list.
  - Every `Need Attention` item must include `Attention reason:` and `Next action:`; if the reason is unknown, use `Needs triage` and state what evidence is missing.
  - At final output, write the final state through the report dashboard browser hook when available and verify current cards/state through the dashboard hook/localStorage when available.

At the end, clearly report repos already fixed, repos changed, verification performed, pushed branches, PR links, reviewer assignment results, Jira ticket links/status transitions (including To Do/In Progress/Approved moves), ticket visibility verification results (including the assignee-filtered board URL), approval polling results, notifications sent, the single report dashboard URL https://thiennp.github.io/report/, dashboard logging status, and blockers/auth failures. In the blocked section, include one clickable Jira link per blocked item and the fresh recheck result for that run.
- Final output must include a single `Stable blocker summary` section when applicable: blocker fingerprint, first-seen timestamp, last-verified timestamp, unchanged/changed verdict, evidence source, notification state (`sent`, `failed`, or `suppressed`), and explicit confirmation that no PR/Jira/repository mutations were performed in this run.

Slack blocked notification requirement (updated 2026-06-03):
- Whenever the automation becomes blocked, immediately notify Thien Nguyen on Slack in a direct message after the fresh blocker evidence is captured and before waiting for human action.
- This applies to hard-gate blockers, credential/auth/client-certificate blockers, Jira/Bitbucket/package-audit access blockers, local-only environment blockers, repository/scanner blockers, missing staging route/login/test-data blockers, and item-level `Block` / `Needs triage` outcomes.
- Use the Slack connector/send capability when available. Resolve Thien Nguyen as the recipient by current Slack user or Slack user search, then send one DM per unique blocked run/item. Do not post blockers into public channels unless Thien explicitly asks for that.
- The Slack message must include: automation name, blocked step number, blocked Jira ticket URL when present, repo/branch/file/package/advisory when present, `Block reason: ...`, `Actionability: ...`, exact next action/owner, report dashboard URL https://thiennp.github.io/report/ when available, and the evidence path or source URL.
- For multiple blockers, send separate Slack DMs for blockers with different actionability or owners, then include a compact count in the final run output.
- Do not send secrets, token values, raw credential file contents, cookies, client-certificate material, or full command environments to Slack.
- If Slack send/auth is unavailable, record `Slack blocked notification failed` with the concrete error in memory, the report dashboard when available, memory, and final output. Continue only if the underlying automation policy allows continuing; for hard gates, stop after recording/reporting the Slack notification failure.
- Add `Slack notification sent:` or `Slack notification failed:` to the saved memory summary and final output for every blocked item.
- Slack capability check must run once per automation run. If no Slack send tool is available, set `slack_send_capability=unavailable` for this run, record one concrete failure evidence entry, and suppress repeated per-step send retries; keep blocker status honest as `notification degraded`, not `notification sent`."
status = "ACTIVE"
rrule = "RRULE:FREQ=WEEKLY;BYHOUR=8;BYMINUTE=0;BYDAY=SU,MO,TU,WE,TH,FR,SA"
model = "gpt-5.5"
reasoning_effort = "high"
execution_environment = "local"
cwds = ["/Users/thien.nguyen/Documents/Codex/2026-05-06/check-open-thread-on-slack-https"]
created_at = 1778053386681
updated_at = 1780991032851
