version = 1
id = "agent-report-server-guard"
kind = "cron"
name = "Agent Report server guard"
prompt = "Check whether the Agent Report server is running on http://localhost:3100/. Run this command from the local machine: `AGENT_REPORT_SEND_STATUS=changes /Users/thien.nguyen/thiennp.github.io/agent-report/scripts/ensure-agent-report-server.sh`. The command should start the server if it is down, send an Agent Report message only when it starts the server or hits a blocker, and stay quiet when the server is already healthy. After running the command, report a concise result for this automation run, including whether the server was already healthy, started, or blocked, and the log path `/Users/thien.nguyen/thiennp.github.io/agent-report/logs/autostart.log` when useful."
status = "ACTIVE"
rrule = "FREQ=MINUTELY;INTERVAL=30"
model = "gpt-5.5"
reasoning_effort = "minimal"
execution_environment = "local"
cwds = ["/Users/thien.nguyen/thiennp.github.io/agent-report"]
created_at = 1780398437839
updated_at = 1780398437839
