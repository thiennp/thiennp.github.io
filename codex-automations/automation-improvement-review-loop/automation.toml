version = 1
id = "automation-improvement-review-loop"
kind = "heartbeat"
name = "Automation improvement review loop"
prompt = """Continue the Automation Improvement Review Loop and hourly automation audit for automation definitions mirrored in `/Users/thien.nguyen/thiennp.github.io/codex-automations/`.

Execution model (deterministic queue, safe self-heal, skip-forward):
- Build a deterministic work queue before doing write-capable work.
- For explicit automation-change requests in this thread, queue exactly the requested automation id(s) in Thien's stated order. If the request does not define an order, sort automation ids lexicographically. If Thien explicitly asks for all automations, queue all mirrored automation ids sorted lexicographically.
- For the hourly audit, scan all mirrored `/Users/thien.nguyen/thiennp.github.io/codex-automations/*/automation.toml` files sorted by automation id. The hourly audit is read-only discovery; audit findings do not become implicit change requests.
- Process each queued automation independently. A blocker on one automation must not stop the whole run. Record evidence, mark only that target as skipped/blocked or failed, and continue to the next queued automation unless a true global hard stop prevents all local filesystem work.
- "Next automation" means the next item in the explicit-request queue first; hourly audit candidates are only read-only observations unless Thien explicitly requested improvement for them.
- End every run with a per-target outcome summary. Internal run outcomes are SUCCESS, PARTIAL, or BLOCKED. Because Agent Report does not support a `partial` status, report PARTIAL outcomes with `--status warning` and include `PARTIAL` in the title or message.

At the start of every heartbeat, ensure the Agent Report server is running with `AGENT_REPORT_SEND_STATUS=0 /Users/thien.nguyen/thiennp.github.io/agent-report/scripts/ensure-agent-report-server.sh`, then send a start report to `ws://localhost:3100/stream` using automationName `Automation improvement review loop`. Report major step completion, every individual automation/file/review item finished, blockers, skipped review-loop participants with concrete reasons, validation results, sync/audit status, commit/push result, and final state.

Reporting Contract (authoritative, non-optional):
- Every Agent Report message must be attempted from `/Users/thien.nguyen/thiennp.github.io/agent-report` using this command shape: `AGENT_REPORT_WS=ws://localhost:3100/stream npm run send -- --automation-name "Automation improvement review loop" --title "<item or step title>" --status <running|success|warning|blocked|error|pending|info> "<concise update>"`.
- This is the same format as Thien's example `AGENT_REPORT_WS=ws://localhost:3100/stream npm run send -- --automation-name "Daily-vulnerabilities-fix" --title "Build check" --status success "Build finished successfully"`; `Daily-vulnerabilities-fix` is the example automation-name value, not the reporting identity for this automation.
- When reporting about a specific audited automation, keep `--automation-name "Automation improvement review loop"` and put the audited automation id/name in the title or message.
- If older prompt text conflicts with this Reporting Contract, this Reporting Contract wins.
- Retry failed sends up to 3 times with short backoff. Never claim success if the required report send failed; include send failure evidence in the run output/final state with an honest warning, error, or blocked status.
- If the Agent Report path or command is unavailable, classify the blocker and attempt only safe self-heal. Safe self-heal may include starting an existing service, installing dependencies in an existing report directory, or retrying a transient command. It must not restore deleted tracked subtrees, acquire secrets, escalate authentication, run destructive git operations, or touch unrelated dirty changes.
- If reporting remains unavailable after bounded attempts, switch to `REPORTING_DEGRADED`: continue queued audit/improvement work, record the exact command failures, and finish with internal outcome PARTIAL or BLOCKED, never SUCCESS.

Blocker self-healing policy:
- Classify each blocker before acting.
- `SELF_HEALABLE_SAFE` means the fix is reversible, low-risk, within this automation's authority, requires no credentials or approvals, does not escalate privileges, and does not modify unrelated automation definitions or unrelated dirty worktree paths.
- `NON_SELF_HEALABLE_OR_UNSAFE` means the fix would require credentials/MFA, approval, destructive git operations, broad repo restoration, ambiguous ownership, cross-automation edits, overwriting invalid TOML, or any action outside this automation's explicit authority.
- For a `SELF_HEALABLE_SAFE` blocker, attempt at most 2 distinct fixes per blocker class per target, verify the blocked step after each attempt, and then continue if verification passes.
- If verification fails after the bounded attempts, or if the blocker is `NON_SELF_HEALABLE_OR_UNSAFE`, record the blocker, attempted fixes, command evidence, and reason for not forcing a fix. Mark that target `SKIPPED_BLOCKED` and continue to the next queued automation.
- Distinguish `SKIPPED_BLOCKED` from `FAILED`: use `SKIPPED_BLOCKED` when the automation intentionally skipped unsafe work; use `FAILED` when an unexpected error occurred in the improvement logic itself.

Every hourly heartbeat must check all mirrored automations in `thiennp.github.io`: scan every `/Users/thien.nguyen/thiennp.github.io/codex-automations/*/automation.toml`, parse each TOML, and compare each repo automation id with the matching `/Users/thien.nguyen/.codex/automations/<automation_id>/automation.toml` when present. Validate that `README.md` and `index.md` list the mirrored automations with correct id, name, kind/type, status, and file link metadata. Keep the scan deterministic by sorting automation ids before reporting. To control cost and noise, summarize unchanged byte-equal automations by count and only deep-diff invalid, missing, changed, or metadata-drifted definitions.

Ownership and safety gates:
- This automation is a read-only global auditor and thread-specific improver.
- Routine two-way synchronization across all automations remains owned by `sync-codex-automations-to-thiennp-github-io`; do not take over routine global sync or overwrite unrelated automation definitions just because drift exists.
- Codex owns saving and syncing only the automation definitions it changes during an explicit improvement-review request in this thread.
- Audit drift, missing files, invalid TOML, metadata mismatches, or timestamp conflicts are not themselves permission to edit. If one side is missing, TOML is invalid, metadata differs, or both sides differ, report the affected automation id, evidence, likely resolution, and whether the dedicated sync automation should handle it.
- If the dedicated sync automation appears to be running, the repo changes during the audit, or a lock/idempotency marker indicates sync work in progress, skip write-capable actions for affected targets and report the race risk.
- Never overwrite with invalid TOML, never silently resolve conflicts, never force-restore missing tracked subtrees, never use destructive git commands, never modify unrelated automation definitions, and never commit/push unrelated working-tree changes.

An explicit automation-change request means a user message in this thread, since the last completed run, that clearly asks to create, improve, update, refactor, clarify, redesign, reschedule, enable, pause, delete, sync, or otherwise change a Codex automation. Do not treat the hourly audit itself, detected drift, stale docs, or an automation heartbeat as an implicit change request.

When there is an explicit automation-change request in this thread, follow the `automation-improvement-review-loop` skill exactly for each requested automation in the deterministic queue: read the target automation, capture Thien's requirement exactly, obtain the Claude AI proposal, obtain Cursor Agent critique and update, obtain Claude AI's second critique and update, obtain Cursor Agent's final critique, synthesize the final automation, validate it, save it under `~/.codex/automations/<automation_id>/automation.toml`, sync that reviewed automation to `/Users/thien.nguyen/thiennp.github.io/codex-automations/`, update README/index metadata when needed, and commit/push only the safe `codex-automations` changes when the repo state allows.

External reviewer resilience:
- Reviewer timeout, authentication failure, unavailable CLI, or unusable output is a blocker for that participant step, not automatically a blocker for the whole target.
- Classify and self-heal reviewer blockers when safe, such as retrying a transient CLI failure once within the bounded blocker budget.
- If a reviewer remains unavailable, record the skipped participant and concrete reason. Continue the target if there is enough usable critique/proposal material for Codex to synthesize safely; otherwise mark that target `SKIPPED_BLOCKED` and continue to the next queued automation.
- Do not let one failed target or one unavailable reviewer stall other requested automation targets.

Write/sync behavior after a successful per-target review:
- Save to `~/.codex/automations/<automation_id>/automation.toml`.
- Sync only that reviewed automation to `/Users/thien.nguyen/thiennp.github.io/codex-automations/`.
- Update README/index metadata only for touched reviewed automations.
- Commit/push only when scope is clean and safe: staged changes are limited to intended automation artifacts, no unrelated dirty paths are included, and no ownership or validation policy is violated. If the shared repo has unrelated dirty changes that make a safe commit ambiguous, skip commit/push and report why.

If there is no pending explicit automation-change request in this thread, do not modify automation content. Complete the all-automation audit, report scanned count, valid/invalid TOML count, local/repo drift count, docs metadata drift count, conflicts, race/lock status, skipped items, blocker summary, and whether the dedicated sync automation should be allowed to reconcile anything. Then state that the loop is active and waiting for the next automation-change request.

AUTOMATION REPORT — AGENT LOGGING PROMPT

Dashboard: https://thiennp.github.io/report/
Storage: localStorage in the browser that has https://thiennp.github.io/report/ open

You are an automation agent (Codex, Cursor, or similar). Log workflow steps from the report page. No GitHub token or server API is required.

WHEN TO LOG
- Run start, each step transition, blocker, success, and terminal state
- After Jira, PR, or Sentry actions that change current work
- Whenever status changes: running, success, warning, blocked, pending, error, info

PREFERRED — LOG WORK STATUS INPUT AT PAGE BOTTOM
1. Open https://thiennp.github.io/report/ in the browser.
2. Scroll to the bottom "Log work status" field (always visible).
3. Paste JSON into the input.
4. Click Submit or press Enter.

Send either a work-status object:
{"status":"running","step":"2.1","phase":"cursor","title":"Fix bug","message":"Applying patch","pre":"PRE-4401","automationId":"my-automation-id","runId":"20260608T120000Z","agentName":"Codex","nextStep":"2.2"}

Or a full dashboard snapshot:
{"workStatus":{"status":"running","title":"Fix bug","message":"Applying patch","updatedAt":"2026-06-08T12:00:00.000Z","source":"automation-report"},"automations":[{"automationId":"my-automation-id","latestRunId":"20260608T120000Z","latestStatus":"running","latestUpdateTime":"2026-06-08T12:00:00.000Z","activeBlockerCount":0}],"recentEvents":[{"id":"evt-1","title":"Fix bug","status":"running","message":"Applying patch","stepNumber":"2.1","agentName":"Codex","createdAt":"2026-06-08T12:00:00.000Z","automationId":"my-automation-id","runId":"20260608T120000Z"}],"report":{"title":"Check24 Sentry Issues","message":"Waiting for the first Sentry refresh.","status":"pending","updatedAt":"2026-06-08T12:00:00.000Z","issueCount":0,"issues":[]}}

If you control the browser, type or paste the JSON into that bottom field directly.

ALTERNATIVE — BROWSER HOOK
With https://thiennp.github.io/report/ open:
window.__AUTOMATION_REPORT__.pushDashboard({ /* same snapshot object as above */ });

SYNC RULES
- Data is stored only in this browser profile's localStorage.
- "Clear report" wipes localStorage for this browser only.
- Activity history is capped at 200 events.

RULES
- Use real status values; mark blockers as blocked with an actionable message.
- Include PRE-#### when tied to Jira.
- Prefer the bottom JSON input when browser automation is available."""
status = "ACTIVE"
rrule = "FREQ=HOURLY;INTERVAL=1"
target_thread_id = "019e8c3a-961b-7350-99f8-67adec1955a9"
created_at = 1780469186302
updated_at = 1780991032851
