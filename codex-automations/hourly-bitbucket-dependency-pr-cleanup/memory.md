# Automation Memory

- 2026-06-17: Removed stale local reporting references from `codex-automations/hourly-bitbucket-dependency-pr-cleanup/automation.toml` and normalized the prompt to Daily Magic-only reporting (`https://daily-magic.d.energie.check24.de/thien/`).
- 2026-06-17: Jira key inference is restricted to explicit Jira links, exact Jira branch names, or standalone Jira title tokens so Sentry identifiers like `CHATTY-3` do not trigger Jira exports.
- 2026-06-17: PR `https://bitbucket.org/check24/enrg-tarifvergleich/pull-requests/13850` was repaired by posting the missing follow-up comment and then approving after the user clarified `.cursor` changes should be approved without review.
- 2026-06-18: Last mistake was an incomplete queue sweep. The automation now requires a persisted per-run outcome ledger plus `queue-completeness-monitor` and `ledger-persistence-monitor` gates. Every future mistake must be converted into a named monitor subagent/rule before the run may finish as `done`.
- 2026-06-18: Review round blocked after export-jira-issue-details.sh failed twice for PRE-4416 with Chrome/JXA error "Invalid index" (-1719) even with Jira page open in Chrome. Manual action: reopen a healthy Chrome window/tab authenticated to Jira on PRE-4416 before rerunning.
- 2026-06-18: If a PR previously had Thien or agent approval/request-changes and Bitbucket loses that state while the effective diff is unchanged, the automation must restore the missing state in the current run instead of skipping the PR. Added `review-state-restore-monitor` to enforce this.
- 2026-06-18: Repeated mistake on PRE-4377: Bitbucket accepted `Request changes` before the rich comment composer was visibly open. Added `comment-before-request-changes-monitor` so future runs must verify composer open, comment inserted, signature present, and comment submission confirmed before any request-changes click.
- 2026-06-22: New rule after stale-status mistake: if Thien or agent made a prior approval/request-changes decision and later effective code changes landed after that decision, the automation must re-review and reapply a fresh decision for the new diff even if Bitbucket currently shows `1+ approval` / `No changes requested`. Added `decision-reapply-after-new-commits-monitor`.
- 2026-06-22: Coverage-report churn is not a standalone review blocker. Generated artifacts such as `coverage` output must be treated as non-actionable noise by default unless the artifact itself is the reviewed product or the repo has an explicit enforced rule making it actionable. Added `non-actionable-generated-artifact-monitor`.
- 2026-06-23: Repo-specific review preference for `enrg-web-frontend` Chatty presentation-state code: when a PR introduces shared presentation-state literals like `minimized`, `hidden`, or `full-sheet` (for example PR `https://bitbucket.org/check24/enrg-web-frontend/pull-requests/1159/diff`), prefer a typed `Enum` over multiple exported string constants. Reuse that preference in future reviews of the same area.
- 2026-06-23: New mistake class discovered while re-reviewing the queue: parallel live checks against the same active Chrome tab can race navigation and attach the wrong Bitbucket page body to a PR decision. Added `single-tab-bitbucket-state-monitor` so future runs must perform live Bitbucket state assertions and click sequences sequentially per PR unless each PR is pinned to a separate explicit Chrome tab/window.
