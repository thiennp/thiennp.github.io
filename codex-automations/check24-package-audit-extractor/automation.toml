version = 1
id = "check24-package-audit-extractor"
kind = "cron"
name = "CHECK24 package audit extractor"
prompt = "Use Google Chrome to open `https://sec.check24.de/package-audit?slug=power&vulnerabilityCountsSearch=critical%2Chigh%2Cmedium%2Clow` and extract the current Power package-audit vulnerability data into `/Users/thien.nguyen/sec.check24.de.json` for downstream agents.

Global shared rules:
- Follow `/Users/thien.nguyen/.codex/AGENTS.md` for automation reporting and shared Jira duplicate-check/fingerprint rules. Do not duplicate those shared rules inside this automation prompt.

Critical stability guardrails:
1. Never trust Chrome's active tab. Always target a Chrome tab whose URL contains `sec.check24.de/package-audit` for package-audit work. If none exists, open a new tab/window with the package-audit URL and then target that URL-matched tab explicitly.
2. Before running Chrome JavaScript, verify `View > Developer > Allow JavaScript from Apple Events` is enabled by executing a tiny script against the target tab. If Chrome blocks JavaScript from Apple Events, do not overwrite `/Users/thien.nguyen/sec.check24.de.json`; keep the previous good file and report the blocker.
3. Never overwrite the existing JSON directly during scraping. Write new output to `/Users/thien.nguyen/sec.check24.de.json.tmp` first. Only atomically replace `/Users/thien.nguyen/sec.check24.de.json` after all validation passes.
4. Before replacing the JSON, create/update a backup of the previous good file at `/Users/thien.nguyen/sec.check24.de.json.bak`.
5. The report must never be replaced by an empty or partial scrape unless the page itself visibly and confidently says there are zero package-audit rows. For this CHECK24 Power audit flow, expect 16 rows unless the page visibly shows a different explicit count. If a scrape sees 0 rows, a non-CHECK24 URL, no Details buttons, or an unrelated page such as Jira/Jenkins/Sentry, treat it as a failed scrape and keep the previous good JSON.
6. Validate these invariants before promoting the tmp file:
   - top-level `status` is `ok` or `partial` with explicit errors;
   - `items` is an array;
   - `items.length` is 16 for the current known Power audit report, unless the CHECK24 page visibly shows a different explicit total;
   - every item has `repository`, `filepath`, `branch`, `severity`, `details_text`, `detail_fields`, and `package_audit_details.raw_text`;
   - `details_text` for every item contains `Audit Details`, `BRANCH`, `LAST CHECKED`, `HOW IT WAS SCANNED`, and `Package`;
   - `summary.total_items === items.length`;
   - `summary.with_jira_ticket + summary.without_jira_ticket === items.length`;
   - `summary.with_local_repository + summary.without_local_repository + summary.ignored_local_repository === items.length`;
   - ignored local repositories are exactly `enrg-chatbot` and `enrg-messagesystem` when those items are present;
   - parsed JSON can be read back from disk.
7. If any invariant fails, leave `/Users/thien.nguyen/sec.check24.de.json` unchanged, write the failed candidate to `/Users/thien.nguyen/sec.check24.de.failed.json`, and report the exact failed invariant.
8. Final output must explicitly include: whether the previous file was preserved or replaced, item count, details coverage, package-audit tab URL used, recheck button counts, Jira match counts, local repository counts, ignored repositories, JSON path, backup path, and any failed/blocked guardrail.

Execution flow:
1. Open or find the CHECK24 package-audit URL in Google Chrome and wait for the page to finish loading. Explicitly target the tab with URL containing `sec.check24.de/package-audit` for all package-audit JavaScript.
2. If Chrome shows a login wall, certificate/client-auth prompt, expired session, network error, Apple Events JavaScript blocker, or any other access blocker, do not overwrite the previous report. If no previous report exists, write a blocked JSON only after validating that it is intentionally a blocked-state report.
3. Verify the package-audit page before interacting: URL must contain `sec.check24.de/package-audit`, title/body must indicate CHECK24 Security/package audit, and visible Details/Recheck controls or an explicit zero-results state must be present.
4. Find every visible `Recheck` button on the package-audit page. Click all of them, sequentially if needed. Record `buttons_found`, `buttons_clicked`, `started_at`, `finished_at`, and wait observations.
5. Wait until every recheck has finished. Treat disabled/spinner/loading states as still in progress. After waiting, refresh the same URL-matched CHECK24 tab and wait for the refreshed page to stabilize.
6. Enumerate every vulnerability/item row visible for the current critical, high, medium, and low filters. Refuse to continue if the row count is 0 without a visible explicit zero-results message.
7. For each item, open its `Details` button from the URL-matched CHECK24 tab, read the dialog content, and close it before moving on. Capture all visible detail content including labels, package names, repositories/projects, versions, severity, vulnerability/advisory IDs, paths, dependency chains, scanner/source data, remediation/fixed-version guidance, timestamps, links, status text, and raw notes shown by the page.
8. Every item must include CHECK24 details in all of these fields: `details_text`, `detail_fields`, and `package_audit_details.raw_text`. Parse the package table into `vulnerable_packages`; if the table is awkward but visible in `details_text`, keep the raw text and include the best parsed rows possible.
9. Open `https://c24-energie.atlassian.net/jira/for-you?tab=assigned` in Google Chrome and wait for Jira to finish loading. Jira tab work may use the Jira URL-matched tab, but must never retarget the package-audit scrape to Jira.
10. If Jira is blocked by login/session/network/access, keep package-audit items intact. Add top-level `jira.status: \"blocked\"`, visible blocker evidence, and item-level `jira_ticket: null`, `jira_issue: null`, plus `jira_match.status: \"blocked\"`.
11. Read assigned Jira items visible on the page. Search/scroll/filter as needed within the assigned view to find tickets related to package-audit items. Add `jira_ticket`, `jira_tickets`, and `jira_match` evidence to every item.
12. For every primary matched Jira ticket, open the Jira issue page in Chrome and read visible issue content into `jira_issue` when Apple Events/page access allows it. If issue content cannot be read, keep `jira_ticket` and set per-item `jira_issue.status: \"blocked\"` or `\"error\"` with evidence.
13. For every distinct repository except ignored repositories, resolve the local repository only at root level: `/Users/thien.nguyen/<repository>`. Do not scan nested folders or broad workspace roots.
14. Always ignore `enrg-chatbot` and `enrg-messagesystem` during local repository discovery and cloning. Keep the package-audit/Jira item if present, but set `local_repository.status: \"ignored\"`, `path: null`, `local_url: null`, and `reason: \"Repository intentionally ignored by automation configuration\"`.
15. If `/Users/thien.nguyen/<repository>` exists and is a git repository, verify it with `git -C /Users/thien.nguyen/<repository> remote -v`. Prefer exact git remote evidence containing `bitbucket.org/check24/<repository>`. Add `local_repository.status: \"found\"`, `name`, `path`, `local_url` as a `file://` URL, `remote_url`, `bitbucket_url`, and `matched_by`.
16. If `/Users/thien.nguyen/<repository>` exists but is not a usable git repository or does not match the expected Bitbucket repository, do not overwrite it. Add `local_repository.status: \"blocked\"` or `\"error\"`, reason, and command evidence.
17. If `/Users/thien.nguyen/<repository>` does not exist, open `https://bitbucket.org/check24/workspace/repositories/` in Google Chrome, search/find the repository by exact name, open the repository page, read or infer the clone URL, and clone it to `/Users/thien.nguyen/<repository>`. Prefer SSH clone URLs when visible/available; otherwise use HTTPS. Never overwrite an existing directory.
18. Assemble the full JSON in memory, write it to `/Users/thien.nguyen/sec.check24.de.json.tmp`, validate all guardrail invariants against the tmp file, then promote tmp to `/Users/thien.nguyen/sec.check24.de.json` only if validation passes.
19. After the validated JSON report has been promoted and shown in the final thread response, start remediation work by delegating tickets to subagents. Do not start remediation before the JSON report is shown.

Post-report remediation delegation workflow:
1. Work only from the validated `/Users/thien.nguyen/sec.check24.de.json` report from this run.
2. Create one subagent per actionable Jira ticket/item, with exactly one Jira ticket assigned to each subagent. Use the ticket key as the subagent scope label, for example `PRE-4353 (Cursor Remediation Agent: enrg-app-bo)`.
3. Skip remediation subagents for items whose `local_repository.status` is `ignored`, `blocked`, `missing`, or `error`, or whose package-audit details show no actionable package/fix path. For skipped items, handle Jira as described in the no-fix workflow below.
4. The main automation agent is the orchestrator. Subagents may inspect local files and propose commands, but all code changes must be delegated to Cursor AI. Do not hand-edit application code directly in the orchestrator.
5. Each subagent must work in the item's `local_repository.path` only and must use the Jira ticket key as the branch name exactly, for example `PRE-1234`. If a branch already exists, reuse it after checking its current state; never overwrite unrelated work.
6. Before code work starts for a ticket, use Chrome/Jira to move that Jira ticket to `In Progress`. Do not move ignored, blocked, or not-actionable tickets to In Progress.
7. Instruct Cursor AI to make the dependency/security fix in the local repository for that one ticket. Cursor must own the code changes. The subagent should give Cursor the ticket key, package-audit details, repository path, affected lockfile/path, package names, severity, branch name, and required verification.
8. The branch for every fix must always be exactly the Jira ticket key, for example `PRE-1234`.
9. The commit message for every fix must always start with the Jira key and use this format: `PRE-1234: (fix) - <short human-readable summary>`.
10. After Cursor finishes changes, the subagent must verify the repo state, relevant dependency/audit checks, and tests where feasible. If verification fails, do not create a PR; report the blocker and keep/update Jira according to the no-fix/blocker workflow.
11. When Cursor's change is verified, create a pull request in Bitbucket using Chrome. The PR source branch must be the ticket branch, and the PR title should use the same format as the commit: `PRE-1234: (fix) - <short human-readable summary>`.
12. Add Aleksandar Jaksic and Jens Bachmann as reviewers on every PR. Use Chrome to create/update the PR and reviewer assignment. If reviewer assignment fails, keep the PR but report the failure clearly.
13. After the PR is created successfully, use Chrome/Jira to move the Jira ticket to `In Code Review`. Do not move a ticket to In Code Review unless a PR URL exists.
14. The final remediation output must include one line per ticket: ticket key, repository, branch, commit message, PR URL if created, reviewers assigned, Jira transition performed, verification run, and blocker/no-fix comment if any.

No-fix / not-actionable workflow:
1. If a ticket cannot be fixed because there is no available fixed upstream version, no vendor-supported remediation path, the package-audit Details provide no actionable dependency, or the repository is intentionally ignored, do not create code changes or a PR.
2. Before commenting, open the Jira ticket in Chrome and read previous visible comments/activity. Avoid duplicate comments. If a recent comment already says the same blocker/no-fix reason, do not add another comment; report that the existing comment was reused.
3. If a new comment is needed, keep it short, clear, and understandable to a non-technical human. Avoid jargon and long logs. Example style: `I checked this item again. There is currently no safe update available for the affected package, so I cannot prepare a fix yet. I will leave this ticket in To Do until an update becomes available.`
4. Move the Jira ticket to `To Do` after adding or confirming the no-fix comment. Do not leave not-actionable tickets In Progress.
5. Save the no-fix evidence and comment status in the final output and, if updating the JSON is useful, in an item-level remediation field.

Required JSON additions:
- Top-level `jira` object with `status`, `source_url`, `captured_at`, `assigned_tickets_seen`, `matched_items`, `issues_read`, and `errors`.
- Top-level `repositories` object with `status`, `search_roots: [\"/Users/thien.nguyen\"]`, `clone_root: \"/Users/thien.nguyen\"`, `found`, `cloned`, `ignored`, `blocked`, `ignored_repositories: [\"enrg-chatbot\", \"enrg-messagesystem\"]`, and `errors`.
- Top-level `remediation` object with `status`, `started_at`, `finished_at`, `tickets_total`, `tickets_delegated`, `tickets_skipped`, `prs_created`, `jira_moved_in_progress`, `jira_moved_in_code_review`, `jira_moved_to_do`, and `errors`.
- Summary fields `with_jira_ticket`, `without_jira_ticket`, `with_jira_issue_content`, `without_jira_issue_content`, `with_local_repository`, `without_local_repository`, and `ignored_local_repository`.
- Each item gets `jira_ticket`, `jira_tickets`, `jira_match`, `jira_issue`, `jira_issues`, `local_repository`, `details_text`, `detail_fields`, `package_audit_details`, and optional `remediation` status.

Rules:
- Do not invent values that are not visible or locally verified. Use `null`, empty arrays, and errors when data is absent.
- Jira issue content must be captured from the actual issue page, not inferred from the assigned-list row alone. The assigned-list row is enough for `jira_ticket`; `jira_issue` requires opening/reading the issue.
- If Chrome blocks JavaScript from Apple Events or Jira issue pages cannot be read, record the blocker in `jira.errors`, set per-item `jira_issue.status: \"blocked\"`, and continue with package-audit/local repository data when possible.
- If repository discovery or cloning partially fails, keep package-audit and Jira data intact. Set `repositories.status` appropriately and record item-level evidence.
- Local repository matching must use `/Users/thien.nguyen/<repository>` only. Do not scan nested folders or other roots.
- Never delete, reset, clean, or overwrite existing local repositories or directories. Only clone missing repositories into a nonexistent `/Users/thien.nguyen/<repository>` directory.
- Never create duplicate Jira comments for the same no-fix/blocker reason; read previous comments first.
- Never move a Jira ticket to In Code Review without a PR URL.
- Never create a PR without assigning or attempting to assign Aleksandar Jaksic and Jens Bachmann as reviewers.
- After writing JSON, reopen/read it or parse it to verify validity.
- After the JSON is written and validated, show the JSON content in the final thread response in a fenced `json` code block before starting remediation delegation. If too large, show the top-level summary plus complete item entries as space allows, state that it was truncated, and include the full JSON path."
status = "PAUSED"
rrule = "FREQ=DAILY;BYHOUR=7;BYMINUTE=30;BYSECOND=0"
model = "gpt-5.5"
reasoning_effort = "medium"
execution_environment = "local"
cwds = ["/Users/thien.nguyen/thiennp.github.io/codex-automations"]
created_at = 1780034339700
updated_at = 1780991032851
