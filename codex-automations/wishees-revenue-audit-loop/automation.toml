version = 1
id = "wishees-revenue-audit-loop"
kind = "heartbeat"
name = "Wishees Revenue Audit Loop"
prompt = "Continue the Wishees production revenue loop for the repo at /Users/thien.nguyen/codex-worktrees/chords and the live site https://www.wishees.com/. Use repo-local Git identity Thien Nguyen <nguyenphongthien@gmail.com> for every commit and ensure `git config user.name` and `git config user.email` are set before committing. Do not use any check24.de email and do not use the typo nguyenphonghtien@gmail.com. On each run: 1) inspect git status, latest local main/origin/main commits, and whether there are uncommitted changes; never overwrite user work. 2) Audit live production routes /, /custom-card, /sponsor, /payment, /partners, /feedback, /affiliate-disclosure, /privacy, /earning-report, and /sitemap.xml for HTTP status, expected content, and whether homepage interactive JS works. Use the in-app browser when available to check homepage console errors, ecard wizard opening, referral carry-through with ?ref=maya-bdays, and payment PayPal link attribution. 3) Compare live production with the pushed main commit by checking whether new pages such as /partners and /feedback are live and whether the live sitemap contains them. 4) If live is stale and Vercel CLI is already authenticated and the project is safely linked to the existing Wishees Vercel project, run the production deploy from the repo; do not create or link a new Vercel project non-interactively. If authentication or project linking is missing, report the blocker and the exact next user action. 5) Review monetization and PM signals: custom-card CTA, sponsor CTA, partner program CTA, feedback mailto flows, disclosure clarity, referral attribution, and any public analytics or stats available without secrets. If no analytics provider is connected, say so and recommend the next smallest measurable step. 6) If a safe code/content improvement is obvious and small, implement it, run `npm run lint`, `npm run test`, `npm run build`, and `npm run build:static`, then commit and push to main using the Gmail identity. If the change needs user judgment or credentials, do not guess; report the decision needed. End every run with concise status: live health, deploy state, revenue/feedback observations, changes made, verification, commit/push result, blockers, and next action."
status = "ACTIVE"
rrule = "FREQ=MINUTELY;INTERVAL=15"
target_thread_id = "019e5e9d-7e26-7693-8d0a-b45bcb1475e8"
created_at = 1779706011086
updated_at = 1779707114056
