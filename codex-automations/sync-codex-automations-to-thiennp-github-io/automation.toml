version = 1
id = "sync-codex-automations-to-thiennp-github-io"
kind = "cron"
name = "Sync Codex automations to thiennp.github.io"
prompt = "Sync Codex automations two ways between `/Users/thien.nguyen/.codex/automations/` and `/Users/thien.nguyen/thiennp.github.io/codex-automations/` so they stay portable across devices. For each automation id present on either side, compare `automation.toml` on both sides. If only one side exists, copy it to the missing side instead of treating that as a deletion. If both sides exist and differ, inspect fields such as `updated_at` and sync the newer definition to the older side; if timestamps or intent are unclear, prefer the later `updated_at`, report the conflict resolution explicitly, and do not drop fields silently. After syncing files, update `codex-automations/README.md` and `codex-automations/index.md` if automation ids, names, types, or statuses changed, then commit and push the `thiennp.github.io` repo if repo files changed. Do not copy secrets, browser cookies, tokens, passwords, OTPs, passkey secrets, or raw credential material. Summarize which automations were synced local-to-repo, which were synced repo-to-local, which conflicts were resolved, and the commit/push result.

Global shared rules:
- Follow `/Users/thien.nguyen/.codex/AGENTS.md` for automation reporting and shared Jira duplicate-check/fingerprint rules. Do not duplicate those shared rules inside this automation prompt."
status = "ACTIVE"
rrule = "FREQ=HOURLY;INTERVAL=1"
model = "gpt-5.4"
reasoning_effort = "medium"
execution_environment = "local"
cwds = ["/Users/thien.nguyen/thiennp.github.io"]
created_at = 1778051997059
updated_at = 1780991032851
