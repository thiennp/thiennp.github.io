version = 1
id = "sync-codex-automations-to-thiennp-github-io"
kind = "cron"
name = "Sync Codex automations with thiennp.github.io"
prompt = "Synchronize Codex automations both ways between `/Users/thiennguyen/.codex/automations/` and `/Users/thiennguyen/thiennp.github.io/codex-automations/` so they stay portable across devices. For each automation id, compare `/Users/thiennguyen/.codex/automations/<automation-id>/automation.toml` with `codex-automations/<automation-id>/automation.toml`. If only one side exists, create the missing counterpart from the existing file. If both sides exist and differ, sync the newer intended definition in the correct direction using clear evidence such as `updated_at`, file mtime, and the surrounding repo state; do not silently overwrite when the direction is ambiguous, and report any conflict that needs human judgment. After the file sync, update `codex-automations/README.md` and `codex-automations/index.md` so they match the resulting exported set, then commit and push the `thiennp.github.io` repo if there are repo changes. Do not copy secrets, browser cookies, tokens, passwords, OTPs, passkey secrets, or raw credential material. Summarize which automations were synced local-to-repo, repo-to-local, created on one side, skipped, or blocked, plus the commit/push result.\n\nAgent Report requirement (updated 2026-06-02):\n- At the very start of every run or heartbeat, ensure the Agent Report server is running by executing: AGENT_REPORT_SEND_STATUS=0 /Users/thiennguyen/thiennp.github.io/agent-report/scripts/ensure-agent-report-server.sh\n- Immediately after the ensure step, send a start message to the Agent Report page at http://localhost:3100/ so Thien can see that this automation has started.\n- Always report major step completion, every individual item finished, pause/blocker, and final state to ws://localhost:3100/stream with automationName, title, status, and text. Keep source as terminal; do not put the automation name in source.\n- Item-finished reports are mandatory: whenever an automation sync, conflict resolution, doc update, commit/push step, or other discrete work item completes, immediately send a report before moving to the next item. Do not wait for the final summary.\n- Each item-finished report must include the item name/key, status, key evidence/output, whether the automation continues or stops, and the next item or next step.\n- Preferred command: cd /Users/thiennguyen/thiennp.github.io/agent-report && AGENT_REPORT_WS=ws://localhost:3100/stream npm run send -- --automation-name \"Sync Codex automations with thiennp.github.io\" --title \"<item or step title>\" --status <running|success|warning|error|blocked|pending|info> \"<concise update>\"\n- Use honest statuses: running for in progress, success for completed work, warning for partial/non-blocking issues, error or blocked for stopped work, pending for waiting, info for neutral updates.\n- If the report server or send command fails, mention that in the final output and continue only when the current task can safely run without the report page."
status = "ACTIVE"
rrule = "FREQ=HOURLY;INTERVAL=1"
model = "gpt-5.4"
reasoning_effort = "medium"
execution_environment = "local"
cwds = ["/Users/thiennguyen/thiennp.github.io"]
created_at = 1778050891719
updated_at = 1780396338000
