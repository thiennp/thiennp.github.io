"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generatePropertyGuard = void 0;
const convert_type_to_guard_1 = require("../handlers/convert-type-to-guard");
const generatePropertyGuard = (property, context = {}) => {
    const typeGuard = (0, convert_type_to_guard_1.convertTypeToGuard)(property.type, context);
    const replacedTypeGuard = typeGuard.replace(/isAsserted<\{\}>/g, 'isType({})');
    if (property.isOptional) {
        return `${property.name}: isUndefinedOr(${replacedTypeGuard})`;
    }
    else {
        return `${property.name}: ${replacedTypeGuard}`;
    }
};
exports.generatePropertyGuard = generatePropertyGuard;
//# sourceMappingURL=generate-property-guard.js.map