"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.handleArrayType = void 0;
const ts = __importStar(require("typescript"));
const convert_type_to_guard_1 = require("./convert-type-to-guard");
const handle_union_type_1 = require("./handle-union-type");
const handleArrayType = (typeNode, context) => {
    if (!ts.isArrayTypeNode(typeNode)) {
        throw new Error('Expected ArrayTypeNode');
    }
    let elementType = typeNode.elementType;
    // Unwrap ParenthesizedTypeNode if present
    if (ts.isParenthesizedTypeNode(elementType)) {
        elementType = elementType.type;
    }
    if (ts.isUnionTypeNode(elementType)) {
        const unionGuard = (0, handle_union_type_1.handleUnionType)(elementType, context);
        return `isArrayWithEachItem(${unionGuard})`;
    }
    const elementGuard = (0, convert_type_to_guard_1.convertTypeToGuard)(elementType, context);
    return `isArrayWithEachItem(${elementGuard})`;
};
exports.handleArrayType = handleArrayType;
//# sourceMappingURL=handle-array-type.js.map