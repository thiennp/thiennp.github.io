import type { IFileSystem } from '../../../domain/interfaces';
export declare class FileSystemImplementation implements IFileSystem {
    readFile(filePath: string): Promise<string>;
    writeFile(filePath: string, content: string): Promise<void>;
    writeFilesToSameDirectory(files: Array<{
        fileName: string;
        content: string;
    }>): Promise<void>;
    writeFilesToDirectory(files: Array<{
        fileName: string;
        content: string;
    }>, baseOutputDir: string): Promise<void>;
    fileExists(filePath: string): Promise<boolean>;
    mkdir(dirPath: string, options?: {
        recursive?: boolean;
    }): Promise<void>;
}
//# sourceMappingURL=file-system.implementation.d.ts.map