import type { IESLint, IProcessExecutor, ILogger } from '../../../domain/interfaces';
export declare class ESLintImplementation implements IESLint {
    private readonly processExecutor;
    private readonly logger;
    constructor(processExecutor: IProcessExecutor, logger: ILogger);
    fixFile(filePath: string): Promise<void>;
    fixFiles(filePaths: string[]): Promise<void>;
}
//# sourceMappingURL=eslint.implementation.d.ts.map