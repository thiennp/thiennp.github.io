"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.takeAll = takeAll;
async function takeAll(context) {
    const { includes } = context;
    if (!includes || includes.length === 0) {
        return [];
    }
    // Return the includes as-is, assuming they are already file paths
    return includes;
}
//# sourceMappingURL=takeAll.js.map