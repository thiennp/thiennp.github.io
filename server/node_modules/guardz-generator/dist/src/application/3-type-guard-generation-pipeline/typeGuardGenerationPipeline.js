"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.typeGuardGenerationPipeline = typeGuardGenerationPipeline;
async function typeGuardGenerationPipeline(strategies, context) {
    const results = [];
    for (const { typeName, node } of context.typesToProcess) {
        let code = undefined;
        for (const strategy of strategies) {
            code = await strategy({ typeName, node, config: context.config });
            if (typeof code === 'string' && code.length > 0) {
                results.push({ typeName, code });
                break;
            }
        }
        if (!code) {
            console.warn(`Warning: No type guard could be generated for type: ${typeName}`);
            // Continue processing other types instead of throwing an error
        }
    }
    return results;
}
//# sourceMappingURL=typeGuardGenerationPipeline.js.map