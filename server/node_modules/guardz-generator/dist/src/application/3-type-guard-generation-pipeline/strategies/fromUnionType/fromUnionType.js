"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.fromUnionType = fromUnionType;
const ts = __importStar(require("typescript"));
function fromUnionType(context) {
    const { typeName, node } = context;
    if (!ts.isTypeAliasDeclaration(node))
        return undefined;
    if (!ts.isUnionTypeNode(node.type))
        return undefined;
    const checks = [];
    for (const typeNode of node.type.types) {
        if (typeNode.kind === ts.SyntaxKind.StringKeyword) {
            checks.push('typeof obj === "string"');
        }
        else if (typeNode.kind === ts.SyntaxKind.NumberKeyword) {
            checks.push('typeof obj === "number"');
        }
        else if (typeNode.kind === ts.SyntaxKind.BooleanKeyword) {
            checks.push('typeof obj === "boolean"');
        }
        else if (ts.isLiteralTypeNode(typeNode) &&
            ts.isStringLiteral(typeNode.literal)) {
            // Handle string literal types
            const literalValue = typeNode.literal.text;
            checks.push(`obj === "${literalValue}"`);
        }
        else {
            return undefined;
        }
    }
    if (!checks.length)
        return undefined;
    const guard = `export function is${typeName}(obj: any): obj is ${typeName} {\n  return ${checks.join(' ||\n    ')};\n}`;
    return guard;
}
//# sourceMappingURL=fromUnionType.js.map