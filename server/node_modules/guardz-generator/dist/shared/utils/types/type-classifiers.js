"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.isTypeAlias = exports.isBuiltInType = exports.isInterfaceType = exports.isEnumType = void 0;
const ts = __importStar(require("typescript"));
// Check if a type is an enum
const isEnumType = (typeName, sourceFiles) => {
    for (const sourceFile of sourceFiles) {
        let found = false;
        ts.forEachChild(sourceFile, (node) => {
            if (ts.isEnumDeclaration(node) && node.name.text === typeName) {
                found = true;
            }
        });
        if (found)
            return true;
    }
    return false;
};
exports.isEnumType = isEnumType;
// Check if a type is an interface
const isInterfaceType = (typeName, sourceFiles) => {
    for (const sourceFile of sourceFiles) {
        let found = false;
        ts.forEachChild(sourceFile, (node) => {
            if (ts.isInterfaceDeclaration(node) && node.name.text === typeName) {
                found = true;
            }
        });
        if (found)
            return true;
    }
    return false;
};
exports.isInterfaceType = isInterfaceType;
// Check if a type is a built-in TypeScript type
const isBuiltInType = (typeName) => {
    const builtInTypes = [
        'string',
        'number',
        'boolean',
        'any',
        'unknown',
        'null',
        'undefined',
        'Date',
        'Array',
        'Promise',
        'Map',
        'Set',
        'RegExp',
        'Error',
        'Object',
        'Function',
        'Symbol',
        'BigInt',
        'Partial',
    ];
    return builtInTypes.includes(typeName);
};
exports.isBuiltInType = isBuiltInType;
// Check if a type is a type alias
const isTypeAlias = (typeName) => {
    const typeAliases = [
        'Nullable',
        'NonEmptyString',
        'NonNegativeNumber',
        'PositiveNumber',
        'NonEmptyArray',
    ];
    return typeAliases.includes(typeName);
};
exports.isTypeAlias = isTypeAlias;
//# sourceMappingURL=type-classifiers.js.map