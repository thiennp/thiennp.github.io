import type { IFileSystem } from '../../../domain/interfaces';
import type { GeneratedFile } from '../../../domain/types';
export declare class FileSystemService {
    private readonly fileSystem;
    constructor(fileSystem: IFileSystem);
    writeFilesToSameDirectory(files: GeneratedFile[]): Promise<void>;
    writeFilesToDirectory(files: GeneratedFile[], baseOutputDir: string): Promise<void>;
    fileExists(filePath: string): Promise<boolean>;
}
//# sourceMappingURL=file-system.service.d.ts.map