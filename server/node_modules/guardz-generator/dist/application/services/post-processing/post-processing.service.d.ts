import type { IFileSystem, ILogger, IPrettier, IProcessExecutor, IESLint } from '../../../domain/interfaces';
import type { GeneratedFile } from '../../../domain/types';
export declare class PostProcessingService {
    private readonly fileSystem;
    private readonly prettier;
    private readonly processExecutor;
    private readonly logger;
    private readonly eslint;
    constructor(fileSystem: IFileSystem, prettier: IPrettier, processExecutor: IProcessExecutor, logger: ILogger, eslint: IESLint);
    processFiles(files: GeneratedFile[]): Promise<void>;
    /**
     * Process files with enhanced import fixing
     * This method provides more detailed control over the import fixing process
     */
    processFilesWithImportFixes(files: GeneratedFile[]): Promise<void>;
}
//# sourceMappingURL=post-processing.service.d.ts.map